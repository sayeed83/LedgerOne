# ADR Reference

**Note:** The actual ADR template used to create new decision records lives at [../architecture-decisions/TEMPLATE.md](../architecture-decisions/TEMPLATE.md) — that is the canonical copy-source for new ADRs, kept alongside the ADRs themselves so numbering and status stay consistent.

This file exists here in `templates/` purely as a pointer, so all templates can be found in one place.

**When it should be used:** When you land in `templates/` looking for the ADR template, follow the link above rather than duplicating it here.
