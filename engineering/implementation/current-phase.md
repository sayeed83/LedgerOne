# Current Phase

**Purpose:** The single source of truth for "what phase/module are we actively working on right now." This is the most frequently updated file in [implementation/](.) — check it first before starting new work.

**Contents:** The active phase (referencing [roadmap.md](roadmap.md)), what's in progress, what's blocked, and what's next.

**When it should be used:** Read at the start of any implementation session to orient on current priorities. Update immediately whenever the active phase or its status changes — do not let this file go stale.

---

## Active Phase

**Phase:** 02 — Platform

**Status:** In progress

**Completed:** Phase 00 — Setup (workspace/tooling scaffolding, lint/DB scripts, local dev environment verified end-to-end). Phase 01 — Foundation is deliberately deferred (not started) — the Platform phase was reprioritized ahead of it per explicit direction, since Authentication has no dependency on the ledger/domain-primitive work Foundation covers.

**Completed (continued):** Authentication module (credentials, sessions, login, passwords, MFA — database, business, presentation layers) and Organization module (tenant, company, branch, department — database layer) shipped and frozen. Module ownership boundaries confirmed: Authentication (credentials, sessions, login, passwords, MFA), User Management (profile, status, personal information), Authorization (roles, permissions), Organization (tenant, company, branch, department).

**Completed (continued):** User Management — Database Layer. Canonical `User` model (`apps/api/src/database/schema/user-management.prisma`) — personal identity, contact, `UserStatus` lifecycle (Invited/Active/Suspended/Deactivated per Ch.10.5), cross-module references to Organization (`companyUuid`/`branchUuid`/`departmentUuid`, FK-002 uuid-reference pattern). Deliberately excludes employment fields (employeeCode/designation/joiningDate) — those belong to a future, distinct Employee entity (Payroll module, Ch.75, EMP-004 bounded-context separation). Migration `20260804115244_user_management_create_tables` applied and verified against local MySQL.

**Completed (continued):** User Management — Repository Layer. `apps/api/src/shared/user-management/domain/{aggregates,enums,errors,interfaces}` (User aggregate with Ch.10.5 transition methods, `UserStatus` enum, `IUserManagementRepository`) and `repository/user-management.repository.ts` (`PrismaUserManagementRepository`) mirroring Organization/Authentication's exact repository pattern. Verified against local MySQL via a temporary script (deleted after the run).

**Completed (continued):** User Management — Business Layer. `apps/api/src/shared/user-management/business/` — `create-user`/`invite-user` (Ch.10.6 onboarding), `get-user`, `update-user`, `activate-user`/`suspend-user`/`deactivate-user` (Ch.10.5 transitions), `list-users`, `search-users`, `validate-user-active`, plus `user-management.composition.ts` and `test-support/fixtures.ts`. Every service depends only on `IUserManagementRepository`.

**Completed (continued):** User Management — Presentation Layer. `apps/api/src/shared/user-management/presentation/` (controllers/v1, dto/{requests,responses}, support) and `index.ts` mirroring Organization/Authentication's exact pattern. Mounted at `/api/v1/users` via `module-registry.ts`. Exposes 9 endpoints (create, invite, get, update, activate, suspend, deactivate, list, search) — `validateUserActive` deliberately has no endpoint, mirroring Organization's own `validateTenantIsActive` guard.

**Completed (continued):** User Management — Application Integration. Verified the module-registry/composition-root/server-boot wiring done alongside the Presentation milestone was already complete and correct (no further code changes needed this pass): `module-registry.ts` mounts `createDefaultUserManagementRouter()` at `/api/v1/users` alongside Authentication's `/api/v1/auth` and Organization's `/api/v1/organization`, all three built the same way (composition root → default router factory → `registerModules`). Confirmed live by running the real app (`tsx src/server.ts`) against local MySQL and exercising all 9 endpoints end-to-end with real fixture data (a real Tenant + Company created via Organization's own API) — create, invite, get, update, activate, suspend, deactivate, list (tenant-wide and `?companyUuid=`-filtered), and search all returned correct data; 422 (missing header), 404 (`USR_USER_NOT_FOUND`), and 409 (`USR_DUPLICATE_EMAIL`, `USR_INVALID_STATUS_TRANSITION`) error paths all confirmed live. Re-verified `/health`, `POST /api/v1/auth/login` validation, and `GET /api/v1/organization/tenants/:uuid` against the same running app to confirm Authentication/Organization are unaffected. Fixture data cleaned up afterward. `npm run typecheck -w apps/api` and `npm test -w apps/api` still pass (296 tests, 48 suites).

**Completed (continued):** User Management — API Testing. `engineering/testing/user-management/{UserManagement.postman_collection.json, LedgerOne.postman_environment.json}`, mirroring Authentication's/Organization's exact structure — one collection + one environment, per-resource folder, `pm.collectionVariables.set` UUID chaining, envelope/error-code assertions. Self-contained by design: uses a fixed test `{{tenantId}}`/`{{companyUuid}}` rather than chaining a real Tenant/Company through Organization's collection, since `users.tenant_id`/`users.company_uuid` carry no DB-level FK (FK-002) and the Business layer never validates them cross-module — and because User Management's `X-Tenant-Id` header needs the tenant's *numeric* id, which no HTTP endpoint anywhere exposes (PK-003), mirroring why Authentication's own collection hardcodes `tenantId=1`. Uses `{{$randomEmail}}` so repeated runs never collide on Ch.10.8's email-uniqueness rule. Verified twice via `npx newman run` against the real app (`tsx src/server.ts`) and local MySQL: 26/26 requests, 56/56 assertions, 0 failures both times. Confirmed Authentication/Organization endpoints unaffected. Test data cleaned up after each run.

**Completed (continued):** Authorization — Database Layer. `apps/api/src/database/schema/authorization.prisma` — `Role` (tenant-owned, `00_BUSINESS_RULES.md` Ch.11: name unique per tenant, `RoleStatus` Active/Retired lifecycle, `isSystemRole` flag for ROL-002's standard Roles), `Permission` (platform-owned reference data per MT-005/PRM-001-002: `permissionKey` in `module.resource.action` form per `03_ARCHITECTURE.md` Ch.9.5/Decision 9.9.1, `PermissionAction` enum View/Create/Edit/Approve/Delete — omits `created_by`/`updated_by` as a documented §2.3 platform-reference-table exception), `RolePermission` and `UserRole` join tables (both tenant-owned, in-module real FKs to `Role`/`Permission`, `RESTRICT`). `UserRole.userUuid` is a cross-module FK-002 reference to User Management's `users.uuid` — no DB-level FK. Table set (`roles`, `permissions`, `role_permissions`, `user_roles`) confirmed against `engineering/prompts/02_platform/00_auth.md` §16. Migration `20260804130139_authorization_create_tables` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate deploy` clean, `SHOW TABLES`/`DESCRIBE`/constraint and index inspection confirm dual keys, soft delete, tenant isolation, and FK/index rules per `06_DATABASE_STANDARDS.md`. Repository/Business/Presentation layers intentionally not started — awaiting review per instruction.

**Completed (continued):** Authorization — Repository Layer. `apps/api/src/shared/authorization/domain/{aggregates,entities,enums,errors,interfaces}` — `Role` aggregate (Ch.11.5 lifecycle: `retire()` only, Active→Retired), `Permission`/`RolePermission`/`UserRole` entities (data shape only, no lifecycle), `RoleStatus`/`PermissionAction` enums mirroring the Prisma schema exactly, `IAuthorizationRepository` interface, and `repository/authorization.repository.ts` (`PrismaAuthorizationRepository`) mirroring Organization/User Management's exact repository pattern — persistence only, explicit `tenantId` re-assertion on every tenant-owned query (MT-002), `RepositoryTransaction` support on every mutation, Permission left read-only (find/list, platform-owned per MT-005/PRM-001). Verified via `npm run typecheck -w apps/api` and `npm test -w apps/api` (296 tests, 48 suites, no regressions), plus a temporary script exercising every repository method end-to-end against local MySQL (Role CRUD/lifecycle, Permission find/list, Role↔Permission and User↔Role assign/remove/list including soft-delete-reuse and tenant-isolation checks, `$transaction` support) — script deleted after the run.

**Completed (continued):** Authorization — Business Layer. `apps/api/src/shared/authorization/business/` — 15 use cases: Role (`create-role`, `get-role`, `update-role`, `retire-role`, `list-roles`), Permission (`list-permissions`, `list-permissions-by-module`), Role Permission (`assign-permission`, `remove-permission`, `list-role-permissions`), User Role (`assign-role`, `remove-role`, `list-user-roles`), and guards (`validate-user-permission`, `validate-user-role` — the authoritative Business-layer authorization check per `03_ARCHITECTURE.md` Ch.9.8/Decision 9.9.3). Plus `authorization-errors.ts`/`authorization-types.ts` (Domain re-export seam), `authorization.composition.ts`, and `test-support/fixtures.ts`, all mirroring Organization/User Management exactly. Added Business-triggered Domain errors to the existing `domain/errors/authorization.errors.ts` (same file the Repository milestone started, extended here exactly as Organization's `organization.errors.ts` grew across its own milestones): `DuplicateRoleNameError` (Ch.11.8), `PermissionNotFoundError`, `DuplicatePermissionAssignmentError`, `DuplicateRoleAssignmentError`, `RoleNotAssignableError` (Ch.11.5 — a Retired Role cannot be assigned to a *new* User; existing assignments persist), `PermissionDeniedError` (validateUserPermission's guard failure). `validateUserPermission` aggregates granted Permissions across every net Role a User holds — including Retired Roles, since Ch.11.5 says their existing assignments persist — and is the guard other (not-yet-built) modules' Business layers will call before a permission-gated action. Every service depends only on `IAuthorizationRepository` — no Prisma, Express, or Zod. Verified via `npm run typecheck -w apps/api` and `npm test -w apps/api` (336 tests, 63 suites — 40 new tests across 15 spec files, no regressions).

**Completed (continued):** Authorization — Presentation Layer. `apps/api/src/shared/authorization/presentation/` (controllers/v1, dto/{requests,responses}, support) and `index.ts`, mirroring Organization/User Management's exact pattern. Mounted at `/api/v1/authorization` via `module-registry.ts` (additive change only — Authentication/Organization/User Management mounts untouched). 12 endpoints across Role (`create`/`get`/`update`/`retire`/`list`), Permission (`list`, optionally filtered by `?moduleName=`), Role Permissions (`list`/`assign`/`remove`), and User Roles (`list`/`assign`/`remove`) — `validateUserPermission`/`validateUserRole` deliberately have no endpoints (internal guards other modules' Business layers call directly, not handbook-defined API surface, mirroring User Management's own `validateUserActive`). `GET /permissions` carries no `X-Tenant-Id` requirement (Permission is platform-owned, MT-005); every other route does. Response DTOs expose only `uuid`/`permissionKey`/business fields — never internal `id`/`tenantId`/`roleId`/`permissionId`/`createdBy`/`updatedBy`/`deletedAt`; the Role↔Permission and User↔Role grant responses are synthesized from the controller's already-known external identifiers (path/body) plus the join row's own `uuid`/`createdAt`, since the Domain entities themselves only carry internal bigint FKs for those relations. Added `sendNoContent` (204, no body) to `response-envelope.ts` — this module is the first with `DELETE` endpoints (`removePermission`/`removeRole`), so no prior module's envelope needed it. Verified via `npm run typecheck -w apps/api` and `npm test -w apps/api` (374 tests, 64 suites — 38 new integration tests, no regressions), plus live exercise against the real running app (`tsx src/server.ts`) and local MySQL: create/duplicate-name/get/retire/re-retire(409)/assign-to-retired-role(403) all confirmed live, `/health` and Authentication/Organization/User Management endpoints re-confirmed unaffected, test data cleaned up afterward.

**Completed (continued):** Authorization — Application Integration. Verified the module-registry/composition-root/server-boot wiring done alongside the Presentation milestone was already complete and correct (no code changes needed this pass): `module-registry.ts` mounts `createDefaultAuthorizationRouter()` at `/api/v1/authorization` alongside Authentication's `/api/v1/auth`, Organization's `/api/v1/organization`, and User Management's `/api/v1/users`, all four built the same way (composition root → default router factory → `registerModules`); `server.ts` requires no per-module changes since it only calls `registerModules(app)` generically. Confirmed live by running the real app (`tsx src/server.ts`) against local MySQL and exercising all 12 Authorization endpoints end-to-end with real data: create/get/update/list/retire Role, list Permissions (no tenant header, platform-owned), assign/list/remove Role Permission (including 409 on duplicate grant and 404 on double-removal), assign/list/remove User Role, and retire→re-retire style transition guards — all returned correct status codes and envelope shapes against the live database. Re-verified `/health`, `POST /api/v1/auth/login` validation, `GET /api/v1/organization/tenants/:uuid`, and `GET /api/v1/users` against the same running app to confirm Authentication/Organization/User Management are unaffected. Test data (temp Role, temp seeded Permission, temp grants/assignments) cleaned up afterward; server stopped. `npm run typecheck -w apps/api` and `npm test -w apps/api` still pass (374 tests, 64 suites).

**Deferred (flagged, not fixed — out of scope for an integration-only milestone):** `RolePermissionNotFoundError`'s message embeds the raw internal `roleId`/`permissionId` (e.g. "Permission '3' is not assigned to Role '5'") when the Repository layer's not-found error is constructed from internal ids rather than uuids — a minor internal-identifier leak into an error *message string* (not a JSON field, so PK-003 isn't violated structurally, but the spirit of "never expose bigint ids" is bent). Pre-existing from the approved Repository/Business milestones; fixing it means changing `RolePermissionNotFoundError`'s constructor or its call sites, which is Business/Domain-layer work explicitly out of scope for this Integration-only milestone.

**Documentation correction (this entry only, no code changed):** This file had gone stale — it stopped narrating Phase 03 — Accounting after the Currency Database+Repository milestone (see below) and never recorded that Accounting was, in fact, carried all the way to completion: Tax Group/Tax Rule (Ch.67/68), Account Group/Account — Chart of Accounts (Ch.17/18), Journal Entry + Journal Entry Line (Ch.20), Ledger Entry (Ch.19), and read-only Financial Reports were all subsequently built across every layer (Database, Repository, Business, Presentation, Integration, Frontend, Tests) and are live in the repository today — verified directly against the repo (not this file) on 2026-08-18: migrations present and applied (`20260804184216_accounting_create_tax_group_tax_rule`, `20260804193250_accounting_create_account_group_account`, `20260805054519_accounting_create_journal_entry_ledger`), full business/presentation code under `apps/api/src/shared/accounting/`, frontend screens under `apps/web/src/modules/accounting/` and routes under `apps/web/src/app/(dashboard)/accounting/*`, Postman collections for all of Tax/ChartOfAccounts/JournalEntries/Ledger/FinancialReports under `engineering/testing/accounting/`, and `npm test -w apps/api` passing (818 tests, 133 suites, as of this entry — well beyond the 501/86 last recorded below). **Phase 03 — Accounting is complete.** No further Accounting work is required before proceeding to Phase 04. This entry does not retroactively rewrite the milestone-by-milestone narration below (kept as historical record); it only corrects the phase-level status this file had implied by omission.

**Phase:** 04 — Inventory

**Status:** In progress

**Completed:** Product Category — Database Layer. `apps/api/src/database/schema/inventory.prisma` (new file) — `ProductCategory` model (`00_BUSINESS_RULES.md` Ch.35: hierarchical grouping of Products for organization and category-level defaults). Tenant-owned (`tenantId`, MT-001); `companyUuid` is a cross-module reference (FK-002) to Organization's `companies.uuid` — no DB-level FK, mirroring Financial Year's/Account Group's own `companyUuid` pattern, consistent with Ch.34.9 ("[Product] Depends on: Company"). `parentProductCategoryId` is a real, in-module RESTRICT self-referential FK (Ch.35.10's hierarchy), mirroring Account Group's own self-referential pattern (Ch.18.7 AGP-003). `defaultTaxGroupUuid` is a cross-module reference (FK-002, no DB-level FK) to Accounting's `tax_groups.uuid` (Ch.67), the category-level default Tax Group Ch.35.3/PCT-002 describes. **Handbook Deviation flagged (not implemented, not guessed):** Ch.35.3's category-level "default Unit of Measure" is not modeled — Unit (Ch.36) has no table yet; to be added once Unit has its own Database milestone. `uq_product_categories_company_parent_name_deleted_at` gives partial "unique within hierarchy level" protection (Ch.35.8) — MySQL treats every NULL `parentProductCategoryId` as distinct, so true root-level sibling-name uniqueness is deferred to a later Business-layer milestone, mirroring Financial Year's own partial-overlap-protection precedent. Migration `20260818054128_inventory_create_product_category` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, `SHOW CREATE TABLE product_categories` confirms dual keys, soft delete, self-referential FK (RESTRICT), tenant/company indexes, and the FK-002 uuid-reference pattern per `06_DATABASE_STANDARDS.md`. `npm run typecheck`, `npm run lint` (15 modules scanned), `npm test -w apps/api` (818 tests, 133 suites, no regressions), and `npm run build` (both workspaces) all pass. Repository/Business/Presentation/Integration/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction. Unit (Ch.36) and Product (Ch.34) are the next entities in dependency order, not yet started.

**Completed (continued):** Product Category — Repository Layer. `apps/api/src/shared/inventory/domain/{entities/product-category.entity.ts, errors/inventory.errors.ts, interfaces/inventory-repository.interface.ts}` (new module folder — Inventory's first) and `repository/inventory.repository.ts` (`PrismaInventoryRepository`), mirroring Accounting's own Account Group/Tax Group repository pattern exactly: `ProductCategory` is a plain entity (no lifecycle/aggregate, matching Ch.35.5's "static, low-change reference data"), `IInventoryRepository` scoped to Product Category only this milestone (to be extended one entity at a time for Unit/Product/etc., mirroring `IAccountingRepository`'s own growth, not a separate repository per entity), four methods only (`createProductCategory`, `findProductCategoryByUuid`, `listProductCategories` optionally filtered by `companyUuid`, `updateProductCategory`) — persistence only, no sibling-name-uniqueness validation (Ch.35.8) or default-Tax-Group-inheritance resolution (Ch.35.3/PCT-002), both explicitly deferred to a future Business-layer milestone. Explicit `tenantId` re-assertion on every query (MT-002); `updateProductCategory` uses the `updateMany`+refetch pattern, throwing `ProductCategoryNotFoundError` on zero rows; `RepositoryTransaction` support on the mutating methods; `parentProductCategoryId` (real in-module FK) and `defaultTaxGroupUuid` (cross-module FK-002 reference) accepted as plain values with no cross-repository existence validation, mirroring Accounting's own handling of `financialYearId`/`fromCurrencyId`. Verified via `npm run typecheck`, `npm run lint` (16 modules scanned, up from 15), and `npm test -w apps/api` (818 tests, 133 suites, no regressions — one incidental run hit a pre-existing flake in `test/integration/authorization/authorization.routes.spec.ts`, 2 tests, unrelated to this milestone since no authorization file was touched; an immediate rerun passed clean, confirming test-isolation flakiness against the shared local MySQL instance, not a regression), and `npm run build` (both workspaces). No temporary end-to-end script was run against live MySQL this milestone (Repository-only, no new Business-layer caller to exercise it through yet); the Database milestone's own live-MySQL verification (table shape, keys, indexes) already covers the schema this Repository layer sits on. Business/Presentation/Integration/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product Category — Business Layer. `apps/api/src/shared/inventory/business/{create,get,list,update}-product-category.service.ts` (4 use cases) + matching `.spec.ts` files, plus `inventory-errors.ts` (Domain re-export seam), `inventory.composition.ts`, and `test-support/fixtures.ts`, all mirroring Accounting's Account Group business pattern exactly — every service depends only on `IInventoryRepository`, no Prisma/Express/Zod. Business rule implemented: no two (non-deleted) Product Categories may share a name at the same hierarchy level — same Company and same parent Product Category, including two root-level Categories that both have no parent (Ch.35.8) — enforced in both `createProductCategory` and `updateProductCategory` via new `DuplicateProductCategoryNameError`, added to `domain/errors/inventory.errors.ts` alongside the existing `ProductCategoryNotFoundError`. `updateProductCategory` re-checks this whenever *either* the name or the effective parent changes (not only on rename) — a moved-but-not-renamed Category can equally collide with an existing sibling at its new location; caught by a test during this milestone (an initial version only re-checked on rename) and fixed before completion. A supplied `parentProductCategoryUuid` is resolved first (never trusting a client-supplied internal id, PK-003), throwing `ProductCategoryNotFoundError` if it doesn't exist; `parentProductCategoryUuid`/`defaultTaxGroupUuid` follow the `undefined`-leaves-untouched / explicit-value-or-`null`-sets convention from Account Group's own update service. Neither `companyUuid` (cross-module FK-002 into Organization) nor `defaultTaxGroupUuid` (cross-module FK-002 into Accounting) is validated for existence — this Business layer depends only on its own module's repository, mirroring every other module's handling of cross-module references. No `inventory-types.ts` seam file was created — Product Category has no enum/value-object yet for a future Presentation layer to import; to be added if/when one exists. Verified via `npm run typecheck`, `npm run lint` (16 modules scanned), and `npm test -w apps/api` (836 tests, 137 suites — 18 new tests across 4 new spec files, no regressions — two incidental full-suite runs this milestone hit pre-existing flakes in unrelated integration tests, `authorization.routes.spec.ts` then `accounting.routes.spec.ts`, neither touched by this milestone; both cleared on immediate rerun, consistent with known test-isolation flakiness against the shared local MySQL instance, not a regression). Presentation/Integration/Frontend layers, Unit (Ch.36), and Product (Ch.34) intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product Category — Presentation Layer. `apps/api/src/shared/inventory/presentation/` (controllers/v1, dto/{requests,responses}, support) and `index.ts`, mirroring Accounting's own Account Group presentation pattern exactly. Mounted at `/api/v1/inventory` via `module-registry.ts` (additive change only — Authentication/Organization/User Management/Authorization/Accounting mounts untouched), behind the same `jwtAuthMiddleware` + `createCurrentTenantMiddleware({ rewriteHeaderAs: "decimal" })` pair every other tenant-owned module uses. 4 endpoints: `POST/GET /product-categories`, `GET/PUT /product-categories/:productCategoryUuid`. Response DTO exposes `uuid`/`companyUuid`/`name`/`defaultTaxGroupUuid`/`createdAt`/`updatedAt` — never internal `id`/`tenantId`/`createdBy`/`updatedBy`/`deletedAt`, and deliberately omits `parentProductCategoryId` entirely rather than resolving it to the parent's `uuid` (an extra lookup per row), mirroring Account Group's own carried-forward Handbook Deviation on the identical field. `defaultTaxGroupUuid` is serialized as-is since it is already a cross-module uuid reference at the Domain layer, not an internal id — no PK-003 concern. Error codes: `INV_PRODUCT_CATEGORY_NOT_FOUND` (404), `INV_DUPLICATE_PRODUCT_CATEGORY_NAME` (409), default `INV_DOMAIN_ERROR` (422) — `response-envelope.ts`/`handle-domain-errors.ts`/`domain-error-http-mapping.ts` are this module's own first copies (mirroring every other module's per-module duplication of this support layer, not a shared/common file). No `inventory-types.ts` seam exists yet (still nothing to re-export). Controllers are thin: header/param/body Zod validation, exactly one Business-layer call, response-DTO mapping — no business logic, no direct Prisma access. Verified via `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), and `npm test -w apps/api` (836 tests, 137 suites, no regressions). Three incidental full-suite runs this milestone hit pre-existing flakes in unrelated, untouched integration test files (`organization.routes.spec.ts`, then `authorization.routes.spec.ts` again) — investigated rather than dismissed this time: those specific spec files pass 100% in isolation (fake-repository-backed, no real database dependency, ~0.4s), and a full `npx jest --runInBand` (serialized, single worker) run of all 137 suites passed clean on the first attempt, confirming the flakiness is parallel-jest-worker resource contention in this sandboxed environment, not a data race in this module's code or a regression from this milestone. No API integration test, Frontend, Unit (Ch.36), or Product (Ch.34) work was done — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product Category — API Integration Tests. `apps/api/test/integration/inventory/inventory.routes.spec.ts` (new file/folder — Inventory's first), mirroring Accounting's/Organization's own `<module>.routes.spec.ts` pattern exactly: the real Express router (`createInventoryRouter`) exercised end-to-end via supertest against a bare Express instance wired to a fake `IInventoryRepository` (reused from the Business milestone's own `test-support/fixtures.ts`, not duplicated) — no real database, no repository/business-service called directly from the test. 18 tests across all 4 endpoints: create (missing-tenant-header 422, malformed-body 422, duplicate-sibling-name 409, parent-not-found 404, success 201 with internal-field-exposure assertions), list (missing-tenant-header 422, success 200, `?companyUuid=` filter passthrough), get-by-uuid (malformed-uuid 422, not-found 404, success 200), update (missing-tenant-header 422, malformed-body 422, not-found 404, duplicate-sibling-name-on-rename 409, new-parent-not-found 404, success 200, explicit-null-clears-parent 200). "Unauthorized requests" is covered as the missing/invalid `X-Tenant-Id` header case (422) — genuine JWT/401 coverage only exists at the real-app level (`test/integration/server.spec.ts`), which doesn't yet cover Authorization or Accounting either, so not extending it to Inventory here is consistent with that existing precedent, not a gap introduced by this milestone. "Forbidden tenant access" has no distinct test: this module has no permission-gated route (`createPermissionMiddleware` remains globally unmounted, same as every other module), so there is no 403 case to exercise — documented in the file rather than silently skipped. "Soft-delete visibility rules" are explicitly marked NOT APPLICABLE in the file's own header comment — Product Category has no delete/remove endpoint, Business use case, or Repository method anywhere in this module, so there is nothing to test without inventing an unimplemented feature. Cross-tenant isolation is exercised via the same not-found assertions Organization's own tests use (a real tenant-scoped repository query makes a cross-tenant lookup and a genuinely nonexistent lookup identical from the caller's perspective, both 404, per MT-002) — explicitly labeled in the test names, not a new isolation mechanism. Verified via `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), `npx jest test/integration/inventory/inventory.routes.spec.ts` in isolation (18/18 passing), and two full-suite `npm test -w apps/api` runs from the repo root (**854 tests, 138 suites**, both clean, no regressions — the prior milestone's parallel-jest-worker flake did not reproduce either time). `npm run build` (both workspaces) passes. Frontend, Unit (Ch.36), Product (Ch.34), Warehouse, Stock, Stock Movement, and Inventory Adjustment intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Unit — Database Layer. `apps/api/src/database/schema/inventory.prisma` extended with `Unit` (00_BUSINESS_RULES.md Ch.36: the quantity-measurement convention for a Product — name, symbol, conversion factor to a base Unit). Product Category (Ch.35, previously completed through API Integration Tests) was not modified — confirmed via `SHOW CREATE TABLE product_categories`, byte-for-byte identical before and after this migration. Tenant-owned (`tenantId`, MT-001) with `companyUuid` as a cross-module reference (FK-002, no DB-level FK), mirroring Product Category's own ownership shape. Ch.36.5's "largely platform-provided... with Company-specific custom units as needed" is the identical platform-template-vs-Company-instance ambiguity Account Group's own Ch.18.5 raised — resolved the same way Account Group's own Database milestone resolved it (a carried-forward precedent, not a new decision): a plain tenant/company-owned table, not a nullable-tenant platform/tenant hybrid schema; the platform-template seeding workflow remains an explicitly deferred, not-yet-designed mechanism. `baseUnitId` is a real, in-module `RESTRICT` self-referential FK (Ch.36.1/36.3/36.11's worked example — a "Box of 100" alternate Unit pointing to a "Pieces" base Unit); `null` means the row is itself a base Unit. `conversionFactor` is a nullable `DECIMAL(20,10)` — required only when `baseUnitId` is set; Ch.36.8's "must be a positive number" validation and Ch.36.7 UNT-002's "must be defined before use" rule are both explicitly deferred to a future Business-layer milestone, mirroring Financial Year's/Tax Rule's own deferred validations. `DECIMAL(20,10)` mirrors Exchange Rate's own `rate` column precision/scale exactly — no handbook-documented precision/scale exists for a conversion factor either, the same flagged Handbook Deviation carried forward, not newly invented. `symbol VARCHAR(20)` is an inferred, undocumented column width, flagged inline. `uq_units_tenant_company_name_deleted_at` gives partial duplicate-name protection per Company (SD-004 pattern), mirroring Tax Group's/Account Group's identical constraint. No enum was added — Ch.36 defines no lifecycle/status for Unit (static reference data, no state machine). Migration `20260818063617_inventory_create_unit` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, `SHOW TABLES`/`DESCRIBE units`/`SHOW CREATE TABLE units` confirm dual keys, soft delete, tenant/company indexes, the self-referential `RESTRICT` FK, and the FK-002 `companyUuid` pattern per `06_DATABASE_STANDARDS.md`. `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), `npm test -w apps/api` (854 tests, 138 suites, unchanged — no test code added, consistent with a Database-only stop condition), and `npm run build` (both workspaces) all pass. Repository/Business/Presentation/Integration/Frontend/Tests layers intentionally not started — stop condition for this milestone, per explicit instruction. Product (Ch.34) remains the next entity in dependency order, not yet started.

**Completed (continued):** Unit — Repository Layer. `apps/api/src/shared/inventory/domain/{entities/unit.entity.ts, errors/inventory.errors.ts (extended), interfaces/inventory-repository.interface.ts (extended)}` and `repository/inventory.repository.ts` (extended) — the existing `IInventoryRepository`/`PrismaInventoryRepository` grown by one entity, not a second repository, mirroring how `IAccountingRepository` grew across Accounting's own Repository milestones. Product Category's own entity/errors/interface methods/repository methods were not modified — confirmed via `git diff` showing zero changes to `product-category.entity.ts`, its business services, presentation files, or its integration test. `Unit` is a plain entity (no aggregate/lifecycle, mirroring Product Category — Ch.36.5 defines no state machine). 7 methods added: `createUnit`, `updateUnit`, `findUnitByUuid`, `listUnitsByCompany` (required `companyUuid`, not optional — a distinct method per this milestone's own explicit request, not a filter param on a shared list method), `listBaseUnits` (rows with `baseUnitId IS NULL`, scoped to a Company — a plain persistence filter, not a business rule), `findUnitByName`, `findUnitBySymbol` (both natural-key lookups scoped to `(tenantId, companyUuid)`, mirroring `findAccountByCode`'s identical shape) — persistence only, no duplicate-name checking, no lifecycle rules, no permission checks; `updateUnit` uses the same `updateMany`+refetch pattern as every other module, throwing new `UnitNotFoundError` on zero rows matched; every mutating method accepts the same `RepositoryTransaction` handle. `conversionFactor` is mapped as a plain `.toFixed()`-derived `string | null`, not Accounting's `DecimalValue` Value Object — that type is owned by Accounting's Domain layer and cannot be imported from Inventory's Repository (cross-module Domain/Repository imports are architecturally forbidden, 03_ARCHITECTURE.md Ch.6.7 — only a Business layer may reach into another module, and only via its published contract); introducing an Inventory-owned decimal Value Object was explicitly out of scope for this Repository-only, no-business-logic milestone, flagged rather than guessed. `business/test-support/fixtures.ts`'s `createFakeInventoryRepository` mechanically extended with 7 new `jest.fn()` mocks to keep `IInventoryRepository`'s type satisfied (the same mechanical-only fixture growth pattern every Accounting Repository milestone used) — no `buildUnit` builder added yet, since no Business-layer test consumes one. Verified via `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), and two full-suite `npm test -w apps/api` runs (854 tests, 138 suites both times, unchanged count — no new tests added, consistent with a Repository-only stop condition with no business/presentation caller yet). `npm run build` (both workspaces) passes. No temporary end-to-end script was run against live MySQL this milestone (Repository-only, no new Business-layer caller to exercise it through yet; the Unit Database milestone's own live-MySQL verification already covers the schema this Repository layer sits on). Business/Presentation/Frontend/Tests layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Unit — Business Layer. `apps/api/src/shared/inventory/business/{create,get,update}-unit.service.ts` and `{list-units-by-company,list-base-units}.service.ts` (5 use cases, exactly the 5 requested), every one depending only on `IInventoryRepository` — no Prisma/Express/Zod, no change to the Repository implementation. Product Category's own business services/entity/presentation/tests were not modified (confirmed via `git diff`). Business rule implemented: Ch.36.8's explicit Validation Rule ("Conversion factor must be a positive number") — enforced in both `createUnit` and `updateUnit` whenever a `conversionFactor` is supplied, via new `InvalidUnitConversionFactorValueError`, using a small inline positive-decimal-string check (no leading `-`, not zero) rather than importing Accounting's `DecimalValue` (a cross-module Domain import, architecturally forbidden). **Deliberately NOT enforced, per explicit instruction to enforce only handbook-defined rules:** a duplicate-name/duplicate-symbol check — unlike Product Category's Ch.35.8 ("must be unique within its hierarchy level"), Ch.36 defines no name/symbol uniqueness rule for Unit at all, so none was added here, breaking from Account Group's/Tax Group's own precedent of adding a Business-layer duplicate check purely because a partial DB unique constraint exists. Flagged, explicit consequence: `uq_units_tenant_company_name_deleted_at` (from the Database milestone) will surface as a raw, untyped Prisma unique-constraint violation if a duplicate name is attempted through `createUnit`/`updateUnit` — a known gap, not a silent omission, and reversible in a future milestone if the handbook is amended or the instruction changes. Ch.36.7 UNT-002 ("conversion factor must be defined before an alternate Unit can be used in a transaction") was read as a Sales/Purchase-Order-line-item-usage rule for a not-yet-built module, not a Unit-creation-time validation — not implemented here, not invented. A supplied `baseUnitUuid` is resolved first in both `createUnit` and `updateUnit` (never trusting a client-supplied internal id, PK-003), throwing `UnitNotFoundError` if it doesn't exist; `baseUnitUuid`/`conversionFactor` follow the same `undefined`-leaves-untouched / explicit-value-or-`null`-clears convention Product Category's own `update` service established. `listUnitsByCompany`/`listBaseUnits` are plain passthroughs to their like-named Repository methods (both already company-scoped at the Repository layer — cross-company isolation is therefore structural, not a Business-layer check to add). `domain/errors/inventory.errors.ts` extended with `InvalidUnitConversionFactorValueError` (`UnitNotFoundError` already existed from the Repository milestone). `business/test-support/fixtures.ts` extended with a `buildUnit` builder (the `createFakeInventoryRepository` mock stubs already existed from the mechanical Repository-milestone update — no repository-interface growth this milestone, so no further fixture-type changes needed). No change to `inventory.composition.ts` — the existing `{repository: PrismaInventoryRepository}` shape already satisfies every new service's `deps`. Verified via `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), and `npm test -w apps/api` (**873 tests, 143 suites** — 19 new tests across 5 new spec files covering success paths, not-found, invalid-conversion-factor (zero/negative/non-numeric), explicit-null-clears-without-validation, and cross-company pass-through/isolation, no regressions). One incidental full-suite run hit the same pre-existing parallel-jest-worker flake in `organization.routes.spec.ts` (untouched by this milestone) documented in prior milestones' own entries; an immediate rerun passed clean. `npm run build` (both workspaces) passes. Presentation/Frontend/API-Testing layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Unit — Presentation Layer. `apps/api/src/shared/inventory/presentation/` extended with 5 controllers (`create`/`get`/`update-unit`, `list-units-by-company`, `list-base-units`), request DTOs (`unit-uuid.schema.ts`, `create-unit.dto.ts`, `update-unit.dto.ts`, `list-units-by-company-query.dto.ts`, `list-base-units-query.dto.ts` — `tenant-id-header.schema.ts` reused as-is), and `unit.response.dto.ts`, mirroring Product Category's exact presentation pattern. `index.ts` extended with 5 routes on the already-mounted `/api/v1/inventory` router (no `module-registry.ts` change needed). Product Category's own controllers/DTOs/routes were not modified (confirmed via `git diff`); the Business/Repository layers were not touched either. Endpoints: `POST/GET /units`, `GET /units/base-units`, `GET/PUT /units/:unitUuid` — `GET /units` requires `?companyUuid=` (mirrors `listUnitsByCompany`'s required, not optional, shape) and `GET /units/base-units` mirrors `listBaseUnits` the same way. The static `/units/base-units` route is deliberately registered before the `/units/:unitUuid` param route in `index.ts` (Express matches in registration order; reversing this would make every `base-units` request instead match `:unitUuid="base-units"`) — verified live via a temporary `tsx` script standing up the real router with a stub repository (not a persisted test file, deleted immediately after): all 5 endpoints returned correct status/shape, including confirming `GET /units/base-units` actually reaches `listBaseUnitsController`, not the uuid-param route. Response DTO exposes `uuid`/`companyUuid`/`name`/`symbol`/`conversionFactor`/`createdAt`/`updatedAt` — never internal `id`/`tenantId`/`createdBy`/`updatedBy`/`deletedAt`, and deliberately omits `baseUnitId` entirely (extra lookup per row not paid), mirroring Product Category's own carried-forward `parentProductCategoryId` omission. `conversionFactor`'s decimal-format/positivity validation is deliberately not duplicated in the request DTOs (shape-only `min(1)`) — enforced once, by the Business layer, mirroring `create-exchange-rate.dto.ts`'s identical reasoning for `rate`. New error codes added to this module's own `domain-error-http-mapping.ts`: `INV_UNIT_NOT_FOUND` (404), `INV_INVALID_UNIT_CONVERSION_FACTOR_VALUE` (422); `INV_DOMAIN_ERROR` (422 default) reused as-is. Controllers are thin: header/param/query/body Zod validation, exactly one Business-layer call, response-DTO mapping — no business logic, no direct Prisma import. Verified via `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), and `npm test -w apps/api` (873 tests, 143 suites, unchanged count — no test code added, consistent with a Presentation-only stop condition with no integration tests requested). One incidental full-suite run hit the same pre-existing parallel-jest-worker flake, this time in `accounting.routes.spec.ts` (untouched); an immediate rerun passed clean. `npm run build` (both workspaces) passes. API integration tests and Frontend intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Unit — API Integration Tests. `apps/api/test/integration/inventory/unit.routes.spec.ts` (new file), mirroring Product Category's own `inventory.routes.spec.ts` pattern exactly: the real Express router (`createInventoryRouter`) exercised end-to-end via supertest against a bare Express instance wired to a fake `IInventoryRepository` — no real database, no repository/business-service called directly. Product Category's own test file, and every Database/Repository/Business/Presentation file, were not modified (confirmed via `git status`/`git diff` — the only new file this milestone is this spec file). 24 tests across all 5 endpoints: create (missing-tenant-header 422, malformed-body 422, base-unit-not-found 404, invalid-conversion-factor 422, success 201 with internal-field-exposure assertions, alternate-unit-with-resolved-base-and-factor 201), list-by-company (missing-tenant-header 422, missing-required-companyUuid 422, success 200 with field-exposure assertions, companyUuid-filter-passthrough), list-base-units (missing-tenant-header 422, missing-required-companyUuid 422, success 200 scoped to companyUuid, plus an explicit route-disambiguation test proving `/units/base-units` reaches `listBaseUnitsController` and never `findUnitByUuid` — the static-vs-param route-ordering risk flagged in the Presentation milestone), get-by-uuid (malformed-uuid 422, not-found/cross-tenant-isolation 404, success 200 with field-exposure assertions), update (missing-tenant-header 422, malformed-body 422, not-found/cross-tenant-isolation 404, new-base-unit-not-found 404, invalid-conversion-factor 422, success 200, explicit-null clears `baseUnitUuid` 200). Every response-body assertion checks `id`/`tenantId`/`baseUnitId`/`createdBy`/`updatedBy`/`deletedAt` are `undefined`, per this milestone's own explicit requirement. "Unauthorized"/"forbidden tenant access"/"soft-delete visibility" scoped identically to Product Category's own file's documented reasoning (missing-header 422 stands in for unauthorized; no permission-gated route exists so no 403 case; no delete endpoint exists so soft-delete visibility is not applicable) — restated in this file's own header comment, not silently assumed. Verified via `npm run typecheck` (api + web), `npm run lint` (16 modules scanned), `npx jest test/integration/inventory/unit.routes.spec.ts` in isolation (24/24 passing), and two full-suite `npm test -w apps/api` runs from the repo root (**897 tests, 144 suites**, both clean, no regressions — the prior milestones' parallel-jest-worker flake did not reproduce either time). `npm run build` (both workspaces) passes. Frontend, Product (Ch.34), Warehouse, Stock, Stock Movement, and Inventory Adjustment remain not started.

**Completed (continued):** Unit — Frontend. First real Inventory frontend code in the repo (`apps/web/src/modules/inventory/{components,hooks,screens}` existed only as empty scaffold folders) — mirrors Accounting's Account Group feature end-to-end (its closest analog: a self-referential-parent, Company-scoped reference-data entity), per 08_FRONTEND_STANDARDS.md/04_FOLDER_STRUCTURE.md. Product Category's frontend was NOT built this milestone (out of scope, per explicit instruction) and no completed backend code was modified.

Shared Types: `packages/shared-types/src/inventory/unit.dto.ts` (new — `UnitResponseDto`/`CreateUnitRequestDto`/`UpdateUnitRequestDto`, fields mirror the backend's own `unit.response.dto.ts`/`create-unit.dto.ts`/`update-unit.dto.ts` exactly, no invented fields) + `inventory/index.ts` (new), plus `packages/shared-types/src/index.ts` extended with `export * from "./inventory"` (previously missing entirely — the `inventory` folder had never been wired into the aggregate export).

API Service: `apps/web/src/services/inventory.service.ts` (the file existed but was empty — first real content) — `listUnitsByCompany`/`listBaseUnits`/`getUnit`/`createUnit`/`updateUnit`, all wrapping the existing single `apiClient` (no new HTTP client), no `X-Tenant-Id` header set (API-004: tenant is derived server-side from the JWT, mirroring `accounting.service.ts`'s identical convention).

Hooks: `apps/web/src/modules/inventory/hooks/{use-units,use-unit,use-create-unit,use-update-unit,use-base-units}.ts` (the 5 explicitly requested files — Accounting's own equivalent bundles all 4 Account Group hooks into one file, but the same React Query conventions are followed: query keys `["inventory", "units"|"unit"|"base-units", scope]`, mutations invalidate every affected query key on success). Also added `use-companies-for-select.ts` — a necessary, module-local duplicate of Accounting's own hook of the same name (ARCH-004: a module reads another module's `services/*.service.ts`, never its internals; every module keeps its own copy rather than sharing one), needed by `CompanySelect`.

Validation: `apps/web/src/modules/inventory/schemas/{shared.schema.ts,unit.schema.ts}` (new folder) — `unitFormSchema`'s `conversionFactor` field reuses the Business layer's exact `isPositiveDecimalString` rule (Ch.36.8) as a Zod `.refine()`, rather than a vaguer shape-only check, since "reuse backend validation" was explicit this milestone.

Components: `UnitForm.tsx` (mirrors `AccountGroupForm.tsx`), `BaseUnitSelect.tsx` (mirrors `AccountGroupSelect.tsx`, sourced from `useBaseUnits` rather than the plain unit list — Ch.36.1/36.11: a Unit's own base Unit is always itself a base Unit), `CompanySelect.tsx` and `CompanyContextBar.tsx` (module-local duplicates of Accounting's own identically-named components — every Company-scoped module keeps its own copy of this pattern, not a shared one). Reused as-is, no new primitives: `Drawer`, `LoadingButton`, `Select`, `TextInput`, `ErrorAlert` (`@ledgerone/ui`), `Toolbar`, `SearchBox`, `PageHeader`, `Pagination` (`@/components/ui/`), `DataTable` (`@/components/data/`). `StatusBadge` was not used — Unit has no status/lifecycle enum (Ch.36.5).

Screens: `UnitListScreen.tsx` (mirrors `ChartOfAccountsScreen.tsx`'s Account Groups tab: `CompanyContextBar` → `Toolbar`+`SearchBox`+"New Unit" → `DataTable` (client-side search/paginate, `TBL-003`'s documented small-dataset exception) → `Pagination` → create `Drawer`) and `UnitDetailScreen.tsx` (mirrors `AccountGroupDetailScreen.tsx`: `PageHeader` with "Back" action, `Card` record view with `Skeleton`/`Alert` loading/error states, Edit `Drawer`). All 11 required screen features present: list, create, edit, Base Unit selector, Company selector (via `CurrentCompanyProvider`/`CompanyContextBar`), Conversion Factor input, search, pagination, loading, empty, and error states.

Routes: `apps/web/src/app/(dashboard)/inventory/units/page.tsx` and `.../units/[unitUuid]/page.tsx` (new — both thin FLD-001 shims rendering the Screen component, no logic). `apps/web/src/layouts/navigation.config.ts` extended with a "Units of Measure" nav item and a `/inventory/units/:unitUuid` breadcrumb branch (mirrors the User Management flat-list-plus-one-detail-route pattern) — additive only, every existing nav entry/branch untouched.

Current Company: reuses the existing `CurrentCompanyProvider`/`useCurrentCompany()` (already mounted at the app-shell level) exactly as Accounting does — no new provider created.

**Limitation discovered from the backend (flagged, not fixed — Frontend-only milestone):** `UnitResponseDto` doesn't echo back `baseUnitUuid` (deliberately omitted at the Presentation milestone, PK-003, to avoid an extra lookup per row) — so `UnitDetailScreen`'s edit form cannot prefill a Unit's current base Unit. This is the exact same, already-existing gap Account Group's own `AccountGroupDetailScreen`/`parentAccountGroupUuid` has (not a new problem introduced here) — the edit form's Base Unit field defaults to "None" on every edit, and if the user doesn't deliberately re-select the base Unit, submitting the edit form will clear it. Flagged inline in both `unit.dto.ts` and `UnitDetailScreen.tsx`'s own comments, consistent with how the pre-existing Account Group gap is documented, not silently reproduced.

Verified via `npm run typecheck -w apps/web` (no errors), `npm run lint -w apps/web` (✔ No ESLint warnings or errors), `npm run build -w apps/web` (✓ Compiled successfully — `/inventory/units` and `/inventory/units/[unitUuid]` both present in the route manifest), and `npm run typecheck -w apps/api` (no errors, confirming the backend was unaffected). No backend file, and no Product Category file, was modified (confirmed via `git status`/`git diff`). Product (Ch.34), Warehouse, Stock, Stock Movement, Inventory Adjustment, and Product Category's own frontend remain not started.

**Completed (continued):** Product — Database Layer. `apps/api/src/database/schema/inventory.prisma` extended with `Product` (00_BUSINESS_RULES.md Ch.34: the master-data record describing an item a Company buys, sells, manufactures, or stocks — name, description, code, Product Category, Unit of Measure, Stocked/Non-Stocked classification, Draft/Active/Discontinued lifecycle per Ch.34.5). Product Category and Unit were not modified — confirmed via `SHOW CREATE TABLE product_categories`/`units`, unchanged before and after this migration. Tenant-owned (`tenantId`, MT-001) with `companyUuid` as a cross-module reference (FK-002, no DB-level FK) to Organization's `companies.uuid`, mirroring Product Category's/Unit's own pattern. `productCategoryId` and `unitId`, by contrast, are real, in-module `RESTRICT` foreign keys (FK-001) to `product_categories.id`/`units.id` — a deliberate deviation from the task's own literal `productCategoryUuid`/`unitUuid` field naming, flagged and confirmed with the user before implementation, since Product Category and Unit are both owned by this same Inventory module (not a cross-module reference), and FK-001 requires in-module FKs to target the internal bigint `id`, never a `uuid`; mirrors ProductCategory's own self-referential FK and Unit's own `baseUnitId` FK. `productCategoryId` is required (PCT-001: "every Product must be assigned to exactly one Product Category"); `unitId` is nullable (Ch.34.8: a Unit of Measure is required only for a Stocked Product — a Business-layer concern, not a DB CHECK constraint, mirroring Unit's own nullable `conversionFactor` reasoning). `productCode VARCHAR(32)` (PRD-001, "unique identifying code within the Company") is unique per Company with `deletedAt` in the constraint (SD-004) — reusable after soft delete, mirroring Organization's `companyCode`/`branchCode`/`departmentCode` pattern (no handbook rule forbids reuse the way COA-003/004 does for Chart of Accounts codes). `isStocked` (Boolean, PRD-002) is a plain required column — the Business-layer concern of locking it once transacted (Ch.34.12) is deferred. `status` (`ProductStatus`: Draft/Active/Discontinued) models Ch.34.5's documented lifecycle diagram exactly, mirroring Role's own `RoleStatus` enum pattern; PRD-003 ("cannot be Discontinued while it has non-zero Stock") is a Business-layer validation deferred until Stock (Ch.38) exists. Migration `20260818081300_inventory_create_product` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, `SHOW TABLES`/`DESCRIBE products`/`SHOW CREATE TABLE products` confirm dual keys, soft delete, the two real in-module `RESTRICT` FKs, tenant/company indexes, and the FK-002 `companyUuid` pattern per `06_DATABASE_STANDARDS.md`. `npm run typecheck -w apps/api` passes clean. Repository/Business/Presentation/Integration/Frontend/Tests layers intentionally not started — stop condition for this milestone, per explicit instruction. Warehouse (Ch.37) remains the next entity in dependency order, not yet started.

**Database Layer follow-up:** a strict architecture review (against `00_BUSINESS_RULES.md`/`06_DATABASE_STANDARDS.md`/`04_FOLDER_STRUCTURE.md`, and compared field-by-field with `Account`/`AccountGroup`/`ProductCategory`/`Unit`/`Company`/`Currency`) found the schema otherwise correct — `updated_at` confirmed byte-for-byte identical to the project-wide standard (`NOT NULL`, no DB default, no `ON UPDATE`, Prisma-client-managed) — with exactly one required fix: a missing `status`-inclusive composite index (every other status-bearing tenant/company-scoped entity already has one). Applied: `@@index([tenantId, companyUuid, status], map: "idx_products_tenant_company_status")`, migration `20260818083758_inventory_product_add_status_index` (a single, additive `CREATE INDEX`), reverified via `npm run typecheck -w apps/api` and `SHOW CREATE TABLE products`. No other schema change made.

**Completed (continued):** Product — Repository Layer. `apps/api/src/shared/inventory/domain/{enums/product-status.enum.ts, entities/product.entity.ts}` (both new) and `errors/inventory.errors.ts`/`interfaces/inventory-repository.interface.ts`/`repository/inventory.repository.ts` (all extended) — the existing `IInventoryRepository`/`PrismaInventoryRepository` grown by one entity, not a second repository, mirroring how it grew for Unit and how `IAccountingRepository` grew across Accounting's own Repository milestones. Product Category's and Unit's own entities/errors/interface methods/repository methods were not modified (confirmed via `git diff`). `Product` is a plain entity (no aggregate/lifecycle methods on the entity itself), mirroring Product Category's/Unit's own entity-not-aggregate placement — deliberately chosen over Account's aggregate pattern, per explicit instruction to follow Product Category's/Unit's exact style; `ProductStatus` (`domain/enums/product-status.enum.ts`) mirrors `AccountStatus`'s file placement and shape (`Draft`/`Active`/`Discontinued`, Ch.34.5), used as a plain data field (`AccountGroup.accountType`'s own precedent for an enum-typed field with no transition methods), not as lifecycle-transition support. 6 methods added, exactly the set requested, no more: `createProduct`, `updateProduct`, `findProductByUuid`, `listProductsByCompany`, `findProductByCode`, `findProductByName` — persistence only, no sibling-name/code-uniqueness checking, no Stocked/Unit-required validation (Ch.34.8), no status-transition rule (PRD-003, Ch.34.12), no permission checks. `updateProduct` uses the same `updateMany`+refetch pattern as every other module, throwing new `ProductNotFoundError` on zero rows matched; every mutating method accepts the same `RepositoryTransaction` handle. `productCategoryId`/`unitId` (both real, in-module FKs per the Database milestone) are accepted as plain values with no cross-repository existence validation, mirroring Account's own handling of `accountGroupId`/`parentAccountId`. `findProductByCode`/`findProductByName` are scoped to `(tenantId, companyUuid, ...)`, mirroring `findUnitByName`/`findUnitBySymbol`'s identical natural-key shape. `status` is mapped between the Domain `ProductStatus` enum and Prisma's generated `ProductStatus` enum via the same `as unknown as X` cast Account/AccountGroup/Currency already use for their own enum fields. `business/test-support/fixtures.ts`'s `createFakeInventoryRepository` mechanically extended with the 6 new `jest.fn()` mocks to keep `IInventoryRepository`'s type satisfied (the same mechanical-only fixture growth pattern every prior Repository milestone used) — no `buildProduct` builder added, since no Business-layer test consumes one yet, and no business logic/fixtures were added per explicit instruction. Verified via `npm run typecheck` (api + web, clean), `npm run lint` (layer-boundary check + module-import check, both clean, 16 modules scanned; `next lint`, clean), `npm test -w apps/api` (**897 tests, 144 suites, all passing, no regressions, no flake this run**), and `npm run build` (both workspaces, clean). No temporary end-to-end script was run against live MySQL this milestone (Repository-only, no new Business-layer caller to exercise it through yet; the Product Database milestone's own live-MySQL verification already covers the schema this Repository layer sits on). Business/Presentation/Integration/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product — Business Layer. `apps/api/src/shared/inventory/business/{create,get,update}-product.service.ts` and `list-products-by-company.service.ts` (4 use cases, exactly the 4 requested) + matching `.spec.ts` files, plus `domain/errors/inventory.errors.ts` extended with `DuplicateProductCodeError` and `business/test-support/fixtures.ts` extended with a `buildProduct` builder — every service depends only on `IInventoryRepository`, no Prisma/Express/Zod, no change to the Repository implementation. Product Category's/Unit's own business services/entities/presentation/tests were not modified (confirmed via `git diff`). Business rules implemented, each explicitly authorized (no rule invented because another module has it): (1) PCT-001 ("every Product must be assigned to exactly one Product Category") — `createProduct`/`updateProduct` resolve `productCategoryUuid` first, throwing `ProductCategoryNotFoundError` if unresolvable; (2) Ch.34.8's Unit reference — a supplied `unitUuid` is resolved the same way, throwing `UnitNotFoundError`; whether a Unit is *required* for a Stocked Product was deliberately NOT enforced (not among the explicitly authorized rules); (3) PRD-001 ("every Product must have a unique identifying code within the Company") — enforced via the Repository's own `findProductByCode`, using new `DuplicateProductCodeError`, mirroring `create/update-product-category.service.ts`'s duplicate-name check pattern; re-checked in `updateProduct` only when `productCode` is actually changing. **Deliberately NOT enforced, per explicit instruction:** PRD-003 ("cannot be Discontinued while it has non-zero Stock") and Ch.34.12 ("Stocked classification is fixed once transacted") — neither Stock (Ch.38) nor any transaction history exists yet in this codebase to evaluate them against; `status`/`isStocked` are accepted and passed straight through with no transition/immutability validation, flagged inline rather than silently invented (mirroring Unit's own UNT-002 deferral). No permission logic, no presentation validation, no Prisma-specific logic anywhere in these services. `productCategoryUuid` is `string | undefined` only in `updateProduct` (never nullable — PCT-001 requires exactly one Category at all times); `unitUuid` follows the three-state `undefined`/`null`/`string` convention `update-unit.service.ts`'s own `baseUnitUuid` established. `buildProduct` mirrors `buildProductCategory`/`buildUnit`'s exact fixture-builder shape; `createFakeInventoryRepository` needed no further changes (already fully stubbed from the Repository milestone). Verified via `npm run typecheck` (api + web, clean), `npm run lint` (layer-boundary + module-import checks clean, 16 modules scanned; `next lint` clean), `npm test -w apps/api` (**914 tests, 148 suites** — 17 new tests across 4 new spec files, no regressions, no flake this run), and `npm run build` (both workspaces, clean). Presentation/Controllers/DTOs/Routes/Integration Tests/Frontend layers, and every other Inventory entity, intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product — Presentation Layer. `apps/api/src/shared/inventory/presentation/` extended with 4 controllers (`create`/`get`/`update-product`, `list-products-by-company`), request DTOs (`product-uuid.schema.ts`, `create-product.dto.ts`, `update-product.dto.ts`, `list-products-by-company-query.dto.ts` — `tenant-id-header.schema.ts` reused as-is), and `product.response.dto.ts`, mirroring Unit's exact presentation pattern (its own closest analog: a required-Company-scoped list, an optional cross-entity reference). `business/inventory-types.ts` (new — this module's first Domain-enum re-export seam, mirroring `accounting-types.ts`) re-exports `ProductStatus` for the response/request DTOs' `z.nativeEnum(...)` validation, since Presentation must not import `domain/` directly (Ch.9.3). `index.ts` extended with 4 routes on the already-mounted `/api/v1/inventory` router (no `module-registry.ts` change needed): `POST/GET /products`, `GET/PUT /products/:productUuid` — list registered before the param route, same ordering convention as Product Category's and Unit's own blocks. Product Category's/Unit's own controllers/DTOs/routes were not modified (confirmed via `git diff`); the Business/Repository/Database layers were not touched either. All 4 Business services (`CreateProductService`, `GetProductService`, `UpdateProductService`, `ListProductsByCompanyService`) reused unchanged — controllers only validate the request, invoke one Business service, map Domain → Response DTO, and return the standard envelope; no Prisma access, no business logic, no duplicate validation, no repository calls anywhere in this layer. Response DTO exposes `uuid`/`companyUuid`/`productCode`/`name`/`description`/`isStocked`/`status`/`createdAt`/`updatedAt` — never internal `id`/`tenantId`/`createdBy`/`updatedBy`/`deletedAt`, and deliberately omits `productCategoryId`/`unitId` entirely rather than resolving them to their respective `uuid`s (would require an extra Repository/Business lookup per response row, which a controller restricted to exactly one Business-service call cannot make), the identical carried-forward Handbook Deviation already documented in `unit.response.dto.ts`/`account.response.dto.ts`. `status`/`isStocked` are accepted as plain request fields with only shape validation (`z.nativeEnum`/`z.boolean`) — no transition/immutability validation is duplicated here, since the Business layer itself performs none this milestone (PRD-003/Ch.34.12 deferred, documented at the Business milestone). New error codes added to this module's own `domain-error-http-mapping.ts`: `INV_PRODUCT_NOT_FOUND` (404), `INV_DUPLICATE_PRODUCT_CODE` (409); `ProductCategoryNotFoundError`/`UnitNotFoundError` reuse their already-mapped codes as-is (not Product-specific, no new mapping needed); `INV_DOMAIN_ERROR` (422 default) reused as-is. Verified via `npm run typecheck` (api + web, clean), `npm run lint` (layer-boundary + module-import checks clean, 16 modules scanned; `next lint` clean), `npm test -w apps/api` (**914 tests, 148 suites, unchanged count** — no test code added, consistent with a Presentation-only stop condition with no integration tests requested, no flake this run), and `npm run build` (both workspaces, clean). API integration tests and Frontend intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product — API Integration Tests. `apps/api/test/integration/inventory/product.routes.spec.ts` (new file), mirroring Product Category's (`inventory.routes.spec.ts`) and Unit's (`unit.routes.spec.ts`) own integration test pattern exactly: the real Express router (`createInventoryRouter`) exercised end-to-end via supertest against a bare Express instance wired to the fake `IInventoryRepository` (reused from `test-support/fixtures.ts`, not duplicated) — no real database, no direct Business-service or Repository calls, no Prisma, no MySQL. 22 tests across all 4 endpoints: create (missing-tenant-header 422, malformed-body 422, Product-Category-not-found 404, Unit-not-found 404, duplicate-code 409, success-without-Unit 201, success-with-resolved-Unit 201), list (missing-tenant-header 422, missing-companyUuid 422, success 200 as an array, companyUuid-filter-passthrough), get-by-uuid (malformed-uuid 422, not-found 404, success 200), update (missing-tenant-header 422, malformed-body 422, not-found 404, new-Product-Category-not-found 404, new-Unit-not-found 404, duplicate-code-on-rename 409, explicit-null-clears-Unit 200, success 200). Every 201/200 response is asserted to expose only business fields — `uuid`/`companyUuid`/`productCode`/`name`/`isStocked`/etc. — and never `id`/`tenantId`/`productCategoryId`/`unitId`/`createdBy`/`updatedBy`/`deletedAt`, confirming the Presentation milestone's response-shape decisions hold under real HTTP. "Unauthorized requests" is covered as the missing/invalid `X-Tenant-Id` header case (422), mirroring Product Category's/Unit's own precedent (genuine JWT/401 coverage still doesn't exist for any Inventory route). "Forbidden tenant access" has no distinct test (no permission-gated route exists). "Soft-delete visibility rules" are explicitly marked NOT APPLICABLE in the file's own header comment — Product has no delete/remove endpoint. Cross-tenant isolation is exercised via the same not-found assertions Product Category's/Unit's own tests use. Verified via `npm run typecheck` (api + web, clean), `npm run lint` (layer-boundary + module-import checks clean, 16 modules scanned; `next lint` clean), `npx jest test/integration/inventory/product.routes.spec.ts` in isolation (22/22 passing — two assertions were corrected mid-milestone: `createProduct`'s own `description: input.description ?? null` normalization means the repository call is asserted with `description: null`, not `undefined`, when no description is supplied on create; `updateProduct` has no such `?? null` normalization, so its own passthrough assertions correctly keep `description: undefined`), and two full-suite `npm test -w apps/api` runs from the repo root (936 tests, 149 suites total — first run hit the same pre-existing parallel-jest-worker flake documented in prior milestones' own entries, this time in `authorization.routes.spec.ts` (`DELETE .../permissions/:permissionKey` returning 404 with no response body — untouched by this milestone); an immediate rerun passed clean, 149/149 suites, confirming it was the known flake, not a regression). `npm run build` (both workspaces) passes. No production code (Database/Repository/Business/Presentation/Frontend) was touched this milestone — confirmed via `git status`/`git diff` — only the new test file and this log entry. Frontend intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Product — Frontend. `apps/web/src/modules/inventory/{components,hooks,screens}` extended, mirroring Unit's own frontend feature end-to-end (its closest analog: a Company-scoped, status-bearing reference-data entity with two picker-field dependencies — Account's own AccountForm/AccountDetailScreen pattern for the status-enum/boolean-switch shape). Unit's and Product Category's own frontend/backend files were not modified — confirmed via `git status` (only new files plus five additive edits to shared, already-existing frontend files: `inventory.service.ts`, `inventory-error-messages.ts`, `navigation.config.ts`, `StatusBadge.tsx`, `packages/shared-types/src/inventory/index.ts`).

Shared Types: `packages/shared-types/src/inventory/product.dto.ts` (new — `ProductStatus` enum, `ProductResponseDto`/`CreateProductRequestDto`/`UpdateProductRequestDto`, fields mirror the backend's own `product.response.dto.ts`/`create-product.dto.ts`/`update-product.dto.ts` exactly, no invented fields). **Scope conflict surfaced and resolved with the user before writing code:** `ProductCategorySelect.tsx` (explicitly required by this milestone) needs to fetch Product Categories, but Product Category has no frontend of its own yet (no DTO/service/hook — its own frontend milestone was never run) and the task text otherwise restricted Service/Types additions to Product-only. Presented the conflict and two options; the user chose to add minimal, read-only Product Category plumbing scoped strictly to backing the selector. Added `packages/shared-types/src/inventory/product-category.dto.ts` (new — `ProductCategoryResponseDto` only, mirrors `product-category.response.dto.ts`'s response shape; no request DTOs, since nothing here creates/edits a Product Category) — this is not a Product Category frontend implementation (no screens, forms, or routes), only the read slice Product's own selector needs.

API Service: `apps/web/src/services/inventory.service.ts` extended with `listProductsByCompany`/`getProduct`/`createProduct`/`updateProduct` (named `listProductsByCompany` rather than the task text's literal `listProducts`, mirroring `listUnitsByCompany`'s own established naming precedent for an endpoint that requires, not optionally filters by, `companyUuid`) and one minimal read-only addition, `listProductCategoriesByCompany`, for the `ProductCategorySelect` conflict above — all wrapping the existing single `apiClient`, no `X-Tenant-Id` header set (API-004), no new HTTP client.

Hooks: `apps/web/src/modules/inventory/hooks/{use-products,use-product,use-create-product,use-update-product}.ts` (the 4 explicitly requested files, same React Query conventions as Unit's own hooks: query keys `["inventory", "products"|"product", scope]`, mutations invalidate every affected query key on success) plus `use-product-categories.ts` (minimal, read-only, backs `ProductCategorySelect` only).

Validation: `apps/web/src/modules/inventory/schemas/product.schema.ts` (new) — `productFormSchema` mirrors the backend's `create-product.dto.ts`/`update-product.dto.ts` field shapes/limits exactly (`productCode` ≤32, `name` ≤150, `description` ≤500, per `inventory.prisma`'s column widths) and reuses `shared.schema.ts`'s existing `uuidSchema`. No rule invented beyond what the backend's Presentation-layer shape validation already expresses — the backend's own Business layer performs no status-transition/immutability validation this milestone (PRD-003/Ch.34.12 deferred), so none was added client-side either.

Components: `ProductForm.tsx` (mirrors `AccountForm.tsx` — its closest analog: a status-enum `Select` plus a boolean `Switch` alongside two picker fields), `ProductCategorySelect.tsx` and `UnitSelect.tsx` (both mirror `BaseUnitSelect.tsx`/`AccountGroupSelect.tsx`'s exact pattern; `UnitSelect` sources from the existing `useUnits` — every Unit, not only base Units, since Ch.34.8's Unit-of-Measure reference carries no base-Unit-only restriction the way Unit's own `baseUnitUuid` does). Reused as-is, no new primitives: `CompanySelect`/`CompanyContextBar` (already existed from Unit's milestone), `Drawer`, `LoadingButton`, `Select`, `Switch`, `Textarea`, `TextInput`, `ErrorAlert` (`@ledgerone/ui`), `Toolbar`, `SearchBox`, `PageHeader`, `Pagination`, `StatusBadge` (`@/components/ui/`), `DataTable` (`@/components/data/`). `StatusBadge`'s existing platform-wide status-color map (`apps/web/src/components/ui/StatusBadge.tsx`) extended with `DISCONTINUED: "danger"` — `DRAFT`/`ACTIVE` were already mapped from Organization/Accounting's own statuses and needed no change.

Screens: `ProductListScreen.tsx` (mirrors `UnitListScreen.tsx`: `CompanyContextBar` → `Toolbar`+`SearchBox`+"New Product" → `DataTable` (client-side search/paginate, `TBL-003`'s documented small-dataset exception, columns for code/name/stocked/status with `StatusBadge`) → `Pagination` → create `Drawer`) and `ProductDetailScreen.tsx` (mirrors `AccountDetailScreen.tsx`'s record-view shape with `UnitDetailScreen.tsx`'s simpler no-lifecycle-buttons flow, since Product has no dedicated activate/deactivate endpoints — `status` is a plain field on the same `PUT` the rest of the form uses). All required screen features present: list, create, edit, Product Category selector, Unit selector, status display via `StatusBadge`, Company selector (via `CompanyContextBar`), search, pagination, loading, empty, and error states.

Routes: `apps/web/src/app/(dashboard)/inventory/products/page.tsx` and `.../products/[productUuid]/page.tsx` (new — both thin FLD-001 shims). `apps/web/src/layouts/navigation.config.ts` extended with a "Products" nav item (reuses the existing `ListIcon` — no `PackageIcon` exists in `@ledgerone/ui`'s icon set, and inventing a new icon primitive was out of scope) and a `/inventory/products/:productUuid` breadcrumb branch, mirroring Unit's own flat-list-plus-one-detail-route pattern — additive only, every existing nav entry/branch untouched.

Company Context: reuses the existing `CompanyContextBar`/`CurrentCompanyProvider` exactly as Unit does — no new selector implementation.

**Limitations discovered from the backend (flagged, not fixed — Frontend-only milestone):** (1) `ProductResponseDto` doesn't echo back `productCategoryUuid`/`unitUuid` (deliberately omitted at the Presentation milestone, PK-003) — so `ProductDetailScreen`'s edit form cannot prefill a Product's current Product Category or Unit; both fields default empty/"None" on every edit, and since `productCategoryUuid` is required (PCT-001), the operator must reselect it on every edit or the submission fails client-side validation — the identical, already-existing gap Unit's own `baseUnitUuid`/Account's own `accountGroupUuid`/`parentAccountUuid` have, not a new problem. (2) Product Category has no frontend of its own yet, surfaced and resolved with the user as described above under Shared Types.

Verified via `npm run typecheck -w apps/web` (no errors), `npm run lint -w apps/web` (✔ No ESLint warnings or errors), `npm run build -w apps/web` (✓ Compiled successfully — `/inventory/products` and `/inventory/products/[productUuid]` both present in the route manifest), and `npm run typecheck -w apps/api` (no errors, confirming the backend was unaffected). `git status` confirms no Database/Repository/Business/Presentation/API-integration-test file, and no Product Category implementation file, was touched — only new frontend files plus five additive edits to already-existing shared frontend files. Warehouse, Stock, Stock Movement, Inventory Adjustment, and Product Category's own frontend remain not started — Warehouse was explicitly not started per instruction to stop after Product's frontend.

**Completed (continued):** Warehouse — Database, Repository, Business, Presentation, and API Integration Tests layers (Ch.37, all backend layers, run across separate milestones prior to this entry — not previously logged in this file; recorded here retroactively so the file stops going stale, mirroring the earlier Accounting documentation-correction entry's own reasoning). **Handbook Deviation flagged and resolved with the user at the Database milestone:** WHS-001/Ch.37.9/37.10 make Warehouse a child of Branch, not Company directly, contradicting that milestone's own prompt text (which asked for a `companyUuid` reference mirroring Product/Unit/Product Category) — the user chose the handbook-compliant option, so `Warehouse` carries `branchUuid` (FK-002, cross-module reference to Organization's `branches.uuid`), not `companyUuid`. Table `warehouses`: `warehouseCode`/`name` each unique per Branch including `deletedAt` (Ch.37.8, SD-004), `WarehouseStatus` enum (Active/Inactive, Ch.37.5, mirrors `BranchStatus`'s two-value convention rather than Product's three-value one). Repository: 6 methods on the existing `IInventoryRepository`/`PrismaInventoryRepository` (`createWarehouse`, `updateWarehouse`, `findWarehouseByUuid`, `findWarehouseByCode`, `listWarehousesByBranch`, `listWarehousesByTenant`), persistence only. Business: `create/get/update-warehouse.service.ts` + `list-warehouses-by-branch.service.ts`, enforcing Ch.37.8's per-Branch `warehouseCode`/`name` uniqueness (`DuplicateWarehouseCodeError`/`DuplicateWarehouseNameError`, the latter via `listWarehousesByBranch` + an in-memory scan since no `findWarehouseByName` method exists, mirroring Product Category's own duplicate-name pattern) — re-checked on update only when the relevant field actually changes. WHS-002 (no deactivation with non-zero Stock) and WHS-003 (a Company must have ≥1 Warehouse if any Product is Stocked) deliberately not enforced — no Stock (Ch.38) table exists yet. Presentation: 4 endpoints (`POST/GET /warehouses`, `GET/PUT /warehouses/:warehouseUuid`), response DTO exposes `uuid`/`branchUuid`/`warehouseCode`/`name`/`description`/`status`/`createdAt`/`updatedAt` only; error codes `INV_WAREHOUSE_NOT_FOUND` (404), `INV_DUPLICATE_WAREHOUSE_CODE`/`INV_DUPLICATE_WAREHOUSE_NAME` (409). API Integration Tests: `apps/api/test/integration/inventory/warehouse.routes.spec.ts`, 19 tests across all 4 endpoints, mirroring Product's own integration-test pattern exactly (hidden-field assertions, exact repository-argument assertions, branch-isolation coverage for the list endpoint). All backend layers verified via `npm run typecheck`, `npm run lint` (16 modules scanned), `npm test -w apps/api` (968 tests, 154 suites at the Integration Tests milestone, no regressions — the shared local-MySQL parallel-jest-worker flake documented in prior entries recurred at least once across these milestones in unrelated, untouched integration-test files and cleared on immediate rerun each time, consistent with the established pattern, not a regression), and `npm run build` (both workspaces).

**Completed (continued):** Warehouse — Frontend. `apps/web/src/modules/inventory/{components,hooks,screens}` extended, mirroring Product's own frontend feature end-to-end (its closest analog: a status-bearing, picker-dependent reference-data entity) — one structural difference from every prior Inventory frontend: Warehouse's real parent is Branch, not Company (WHS-001), so a `BranchSelect` sits alongside the existing Company context rather than a second Company-scoped picker. Product's/Unit's/Product Category's own frontend and every backend file were not modified — confirmed via `git status` (only new files plus six additive edits to shared, already-existing frontend files: `inventory.service.ts`, `inventory-error-messages.ts`, `navigation.config.ts`, `StatusBadge.tsx`, `packages/shared-types/src/inventory/index.ts`, and this file).

Shared Types: `packages/shared-types/src/inventory/warehouse.dto.ts` (new — `WarehouseStatus` enum, `WarehouseResponseDto`/`CreateWarehouseRequestDto`/`UpdateWarehouseRequestDto`, fields mirror the backend's own `warehouse.response.dto.ts`/`create-warehouse.dto.ts`/`update-warehouse.dto.ts` exactly, no invented fields). Unlike Product's own response DTO, no field is omitted for an internal-FK reason — `branchUuid` is the entity's own cross-module reference, not an in-module id, so it round-trips as-is; no comparable gap to flag here.

API Service: `apps/web/src/services/inventory.service.ts` extended with `listWarehousesByBranch`/`getWarehouse`/`createWarehouse`/`updateWarehouse`, all wrapping the existing single `apiClient` (no new HTTP client), no `X-Tenant-Id` header set (API-004, same pre-existing convention every other Inventory service function already follows — see Backend limitations below).

Hooks: `apps/web/src/modules/inventory/hooks/{use-warehouses,use-warehouse,use-create-warehouse,use-update-warehouse}.ts` (the 4 explicitly requested files, same React Query conventions as Product's own hooks: query keys `["inventory", "warehouses"|"warehouse", scope]`, mutations invalidate every affected query key on success). No extra module-local hook file was added for `BranchSelect` (unlike Product's own milestone, which added `use-product-categories.ts` for `ProductCategorySelect`) — the branch-fetching `useQuery` is inlined directly inside `BranchSelect.tsx` itself, reusing Organization's already-existing `listBranchesByCompany` service function (no new backend endpoint, no new shared-types addition needed), since the required-files list named `BranchSelect.tsx` as the only new component and this stays within it.

Validation: `apps/web/src/modules/inventory/schemas/warehouse.schema.ts` (new) — `warehouseFormSchema` mirrors the backend's `create-warehouse.dto.ts`/`update-warehouse.dto.ts` field shapes/limits exactly (`warehouseCode` ≤32, `name` ≤150, `description` ≤500, per `inventory.prisma`'s column widths) and reuses `shared.schema.ts`'s existing `uuidSchema`. No rule invented beyond what the backend's own Presentation-layer shape validation already expresses.

Components: `WarehouseForm.tsx` (mirrors `ProductForm.tsx`, simpler — no boolean flag, one picker dependency instead of two) and `BranchSelect.tsx` (mirrors `ProductCategorySelect.tsx`'s exact pattern, but reaches across modules per ARCH-004 — reads Organization's `services/organization.service.ts` directly, exactly as `use-organization-options.ts`'s own `useBranchOptions` already does for User Management, rather than importing anything from `modules/organization/*`). Reused as-is, no new primitives: `CompanyContextBar` (already existed), `Drawer`, `LoadingButton`, `Select`, `Textarea`, `TextInput`, `ErrorAlert` (`@ledgerone/ui`), `Toolbar`, `SearchBox`, `PageHeader`, `Pagination`, `StatusBadge` (`@/components/ui/`), `DataTable` (`@/components/data/`). `StatusBadge`'s existing platform-wide status-color map needed no new entries — `ACTIVE`/`INACTIVE` were already mapped from Branch's own `BranchStatus`; only a clarifying comment was added, no color invented.

Screens: `WarehouseListScreen.tsx` (mirrors `ProductListScreen.tsx`: `CompanyContextBar` → an added `BranchSelect` "Active Branch" row (Warehouse's own extra scoping level beyond Product's Company-only context — no shared "current Branch" context exists platform-wide, so the selected Branch is local screen state) → `Toolbar`+`SearchBox`+"New Warehouse" → `DataTable` (client-side search/paginate, columns for code/name/status with `StatusBadge`) → `Pagination` → create `Drawer`) and `WarehouseDetailScreen.tsx` (mirrors `ProductDetailScreen.tsx`'s record-view/edit-`Drawer` shape exactly). All required screen features present: list, create, edit, Branch selector, Company context, status display via `StatusBadge`, search, pagination, loading, empty, and error states.

Routes: `apps/web/src/app/(dashboard)/inventory/warehouses/page.tsx` and `.../warehouses/[warehouseUuid]/page.tsx` (new — both thin FLD-001 shims). `apps/web/src/layouts/navigation.config.ts` extended with a "Warehouses" nav item (reuses the existing `BuildingIcon` — already imported for the Organization nav item and for `CompanyContextBar`'s own icon; no new icon primitive invented) and a `/inventory/warehouses/:warehouseUuid` breadcrumb branch, mirroring Product's own flat-list-plus-one-detail-route pattern — additive only, every existing nav entry/branch untouched.

**Backend limitations discovered from the frontend (flagged, not fixed — Frontend-only milestone):** (1) Neither `WarehouseResponseDto` nor Organization's own `BranchResponseDto` echoes back a `companyUuid` — so `WarehouseDetailScreen` has no way to derive, from the API response alone, which Company a given Warehouse's Branch belongs to (the same class of gap as Product's own `productCategoryUuid`/`unitUuid` omission, just one hop further up the reference chain). Worked around by scoping the edit form's (disabled, since `branchUuid` is immutable and not part of `UpdateWarehouseRequestDto`) `BranchSelect` fetch by the globally active Company context instead — correct only when that happens to match the Warehouse's real Company; flagged inline in `WarehouseDetailScreen.tsx`'s own comment, not silently assumed correct. (2) Confirmed pre-existing, not new: `inventory.service.ts` sets no `X-Tenant-Id` header on any request (API-004 says the tenant is derived server-side from the JWT) — this was already true for every other Inventory service function before this milestone, so Warehouse's new functions simply follow the same convention rather than introducing a new gap.

Verified via `npm run typecheck -w apps/web` (no errors), `npm run lint -w apps/web` (✔ No ESLint warnings or errors), `npm run build -w apps/web` (✓ Compiled successfully — `/inventory/warehouses` and `/inventory/warehouses/[warehouseUuid]` both present in the route manifest), and `npm run typecheck -w apps/api` (no errors, confirming the backend was unaffected). `git status` confirms no backend (Database/Repository/Business/Presentation/Integration-test) file, and no Product/Unit/Product Category frontend file, was touched — only new Warehouse frontend files plus six additive edits to already-existing shared files (five frontend, one this log). Stock, Stock Movement, Inventory Adjustment, and every other not-yet-started Inventory entity remain not started — stop condition for this milestone, per explicit instruction.

**Phase:** 03 — Accounting Foundation

**Status:** In progress

**Completed:** Financial Year — Database Layer. `apps/api/src/database/schema/accounting.prisma` — `FinancialYear` model (`00_BUSINESS_RULES.md` Ch.5: the annual reporting-cycle definition — `startDate`/`endDate`, `FinancialYearStatus` lifecycle Future/Open/Closing/Closed/Reopened per Ch.5.5). Tenant-owned (`tenantId`, MT-001); `companyUuid` is a cross-module reference (FK-002) to Organization's `companies.uuid` — no DB-level FK, mirroring User Management's own `companyUuid` pattern. `uq_financial_years_company_start_date_deleted_at` gives partial duplicate-start-date protection; true overlap validation (Ch.5.8, FY-002 contiguity) is explicitly deferred to a later Business-layer milestone, since MySQL can't express a date-range-overlap constraint declaratively. Fiscal Period (Ch.6), Financial Closing (Ch.32), Chart of Accounts (Ch.17), and Currency (Ch.7) are deliberately not part of this schema — separate handbook chapters/entities. Migration `20260804142808_accounting_create_financial_year` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, `SHOW CREATE TABLE`/`_prisma_migrations` confirm dual keys, soft delete, tenant-leading composite index, and FK-002 uuid-reference pattern per `06_DATABASE_STANDARDS.md`. `npm run typecheck`, `npm run lint`, and `npm test -w apps/api` (390 tests, 69 suites) all pass with no regressions. Repository/Business/Presentation layers intentionally not started — stop condition for this milestone.

**Completed (continued):** Financial Year — Repository Layer. `apps/api/src/shared/accounting/domain/{aggregates,enums,errors,interfaces}` — `FinancialYear` aggregate (Ch.5.5 lifecycle: `open()` Future→Open, `close()` Open/Closing/Reopened→Closed, `reopen()` Closed→Reopened per FY-004), `FinancialYearStatus` enum mirroring the Prisma schema exactly, `IAccountingRepository` interface, and `repository/accounting.repository.ts` (`PrismaAccountingRepository`) mirroring Organization/User Management/Authorization's exact repository pattern — persistence only (no contiguity/overlap validation per FY-002/Ch.5.8, no closing-process mechanics per Ch.32), explicit `tenantId` re-assertion on every query (MT-002), `RepositoryTransaction` support on every mutation, `companyUuid` handled as a cross-module FK-002 reference (no DB-level FK) mirroring User Management's own pattern. Seven methods only, as scoped: `createFinancialYear`, `findFinancialYearByUuid`, `listFinancialYears` (optionally filtered by `companyUuid`), `updateFinancialYear`, `openFinancialYear`, `closeFinancialYear`, `reopenFinancialYear`. Verified via `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned, up from 14), and `npm test -w apps/api` (390 tests, 69 suites, no regressions), plus a temporary script exercising every repository method end-to-end against local MySQL (create/find/list/update, open→close→reopen lifecycle, not-found error path, cross-tenant isolation) — script deleted after the run. Business/Presentation layers intentionally not started — stop condition for this milestone.

**Completed (continued):** Financial Year — Business Layer. `apps/api/src/shared/accounting/business/` — 8 use cases: `create-financial-year` (overlap validation, FY-002/Ch.5.8), `get-financial-year`, `update-financial-year` (re-validates overlap, excluding self, when either date changes), `list-financial-years` (optionally filtered by `companyUuid`), `open-financial-year`/`close-financial-year`/`reopen-financial-year` (lifecycle transitions enforced via the `FinancialYear` aggregate's `open()`/`close()`/`reopen()`), and the guard `validate-financial-year-open` (Ch.5.7 FY-003 — the check a future Journal Entries module will call before posting, mirroring Organization's `validateTenantIsActive`/User Management's `validateUserActive`). Plus `accounting-errors.ts`/`accounting-types.ts` (Domain re-export seam), `accounting.composition.ts`, and `test-support/fixtures.ts`, all mirroring Organization/User Management/Authorization exactly. Added two Business-triggered Domain errors to the existing `domain/errors/accounting.errors.ts` (same file the Repository milestone started, extended here exactly as Authorization's `authorization.errors.ts` grew across its own milestones): `FinancialYearOverlapError` (FY-002/Ch.5.8) and `FinancialYearNotOpenError` (FY-003 guard failure). Overlap validation is implemented as simple non-overlap (any two Financial Years' date ranges for the same Company must not intersect) — FY-002's stronger "no gap" contiguity requirement (an exact-adjacency check between consecutive years) was deliberately not implemented, since the handbook's own Ch.5.12 stub-period exception for a Company's *first* Financial Year makes "what counts as adjacent" ambiguous enough that encoding it risked inventing behavior beyond what Ch.5.7/5.8 state explicitly; flagged here rather than guessed. `companyUuid` existence is not validated (cross-module FK-002 reference into Organization), mirroring User Management's own `companyUuid` handling. Every service depends only on `IAccountingRepository` — no Prisma, Express, or Zod. Verified via `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` (421 tests, 77 suites — 31 new tests across 8 spec files, no regressions). Presentation layer intentionally not started — stop condition for this milestone.

**Completed (continued):** Financial Year — Presentation Layer. `apps/api/src/shared/accounting/presentation/` (controllers/v1, dto/{requests,responses}, support) and `index.ts`, mirroring Organization/User Management/Authorization's exact pattern. Mounted at `/api/v1/accounting` via `module-registry.ts` (additive change only — Authentication/Organization/User Management/Authorization mounts untouched), behind the same `jwtAuthMiddleware` + `createCurrentTenantMiddleware({ rewriteHeaderAs: "decimal" })` pair as User Management/Authorization. 7 endpoints: `create`/`get`/`update`/`list` Financial Year, plus `open`/`close`/`reopen` transition endpoints — `validateFinancialYearOpen` deliberately has no endpoint (internal guard a future Journal Entries module's Business layer will call directly, not handbook-defined API surface, mirroring User Management's own `validateUserActive`/Organization's `validateTenantIsActive`). Response DTO exposes only `uuid`/`companyUuid`/business fields — never internal `id`/`tenantId`/`createdBy`/`updatedBy`/`deletedAt`. `startDate`/`endDate` accepted as ISO date strings via `z.coerce.date()` on create/update. Error codes: `ACC_FINANCIAL_YEAR_NOT_FOUND` (404), `ACC_INVALID_FINANCIAL_YEAR_STATUS_TRANSITION` (409), `ACC_FINANCIAL_YEAR_OVERLAP` (409), `ACC_FINANCIAL_YEAR_NOT_OPEN` (403, mirrors `TenantNotActiveError`/`UserNotActiveError`'s 403 convention), default `ACC_DOMAIN_ERROR` (422). Verified via `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` (443 tests, 78 suites — 22 new integration tests, no regressions), plus live exercise against the real running app (`tsx src/server.ts`) and local MySQL using a manually-minted RS256 access token (no seeded Authentication user existed to log in with): create/list/get/update/open/re-open(409)/close/reopen/get-missing(404)/no-auth(401) all confirmed live against the real database; `/health`, `GET /api/v1/users`, and `GET /api/v1/authorization/roles` re-confirmed unaffected. Test data cleaned up afterward (`DELETE FROM financial_years ...`); server stopped.

**Completed (continued):** Financial Year — Application Integration. Verified the module-registry/composition-root/server-boot wiring done alongside the Presentation milestone was already complete and correct (no code changes needed this pass, confirmed): `module-registry.ts` mounts `createDefaultAccountingRouter()` at `/api/v1/accounting` alongside Authentication's `/api/v1/auth`, Organization's `/api/v1/organization`, User Management's `/api/v1/users`, and Authorization's `/api/v1/authorization`, all built the same way (composition root → default router factory → `registerModules`); `server.ts` requires no per-module changes since it only calls `registerModules(app)` generically. `accounting.composition.ts` constructs exactly one `PrismaAccountingRepository` instance, called from exactly one place (`createDefaultAccountingRouter`); confirmed no direct Prisma import anywhere in the module outside `repository/accounting.repository.ts`. Confirmed `createPermissionMiddleware` (in `common/middleware/permission.middleware.ts`) remains unmounted anywhere in the app — grepped for its usage, only its own file and a documentation comment in Authorization's `module.manifest.ts` reference it. Confirmed live by running the real app (`tsx src/server.ts`) against local MySQL and exercising all 7 Accounting endpoints end-to-end with real data: create/409-overlap/list/get/update/open/409-re-open/close/reopen/404-missing/401-no-auth all returned correct status codes and envelope shapes against the live database, with `X-Correlation-Id` confirmed present on the response (correlation-id + logging middleware apply globally in `server.ts`, ahead of `registerModules`, so Accounting gets them for free with no per-module wiring). Re-verified `/health`, `POST /api/v1/auth/login` validation, `GET /api/v1/organization/tenants/:uuid`, `GET /api/v1/users`, `GET /api/v1/authorization/roles`, and `GET /api/v1/authorization/permissions` against the same running app to confirm Authentication/Organization/User Management/Authorization are unaffected. Test data (temp Financial Year under a throwaway `companyUuid`) cleaned up afterward; server stopped. `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` still pass (443 tests, 78 suites).

**Completed (continued):** Financial Year — API Testing. `engineering/testing/accounting/{FinancialYear.postman_collection.json, LedgerOne.postman_environment.json}`, mirroring Organization's/User Management's exact structure — one collection + one environment, one folder ("Financial Year"), `pm.collectionVariables.set` uuid chaining, collection-level pre-request/test scripts. 25 requests covering create/get/list/update/open/close/reopen success paths, 4 validation failures (malformed uuid/companyUuid, missing required fields), 1×401, 5×404, 2×409-overlap, 3×409-invalid-transition — a second Financial Year is created purely to exercise overlap/invalid-transition paths without disturbing the primary one's own lifecycle sequence. Two design deviations from User Management's original collection, both documented in the collection's own description: (1) **JWT is required and present on every request** (`Authorization: Bearer {{accessToken}}`) — User Management's/Organization's collections were authored before `jwt-auth.middleware.ts` was mounted ahead of every protected module (Foundation Hardening) and are now stale, 401-ing on every request if run today; verified this empirically before building Accounting's collection. `{{accessToken}}` is a long-lived (10-year) RS256 token pre-signed with the repo's own already-documented dev-only JWT keypair (`.env`'s `JWT_PRIVATE_KEY`) for `tenantId` 900001 — not obtained via a live login, since no seeded Authentication credential is guaranteed to exist in a fresh dev database. (2) **No "missing X-Tenant-Id → 422" test** — `current-tenant.middleware.ts` derives and overwrites `X-Tenant-Id` from the verified JWT claim before this module's own schema ever inspects it (confirmed empirically: a request with a valid token and no client-sent header still succeeds), so that validation path User Management's original collection exercises is no longer reachable once jwt-auth sits in front of a module; replaced with equivalent 422 coverage via malformed body/uuid/query instead. Repeatable-overlap-testing gap solved with a collection-level pre-request script computing a moving date window (`{{fyStartYear}}` etc., derived from `Date.now()`) once per Newman invocation, the date-based equivalent of `{{$randomEmail}}`'s uniqueness trick, since Financial Year has no email-like field to randomize. Verified via two consecutive `npx newman run` invocations against the real running app (`tsx src/server.ts`) and local MySQL: **25/25 requests, 107/107 assertions, 0 failures both times**, each run landing on a different generated uuid/date window, confirming true repeatability rather than accidental idempotence. Confirmed Authentication/Organization/User Management/Authorization endpoints unaffected after both runs. Test data (12 rows total across both runs, scoped to the fixed test `companyUuid`) cleaned up afterward via direct `DELETE FROM financial_years`; server stopped; all script/token-minting scaffolding used to build the environment's `accessToken` deleted — nothing left behind beyond the two committed JSON files. `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` re-confirmed unaffected (443 tests, 78 suites — this milestone touched no application code).

**Completed (continued):** Fiscal Period — Database Layer. `apps/api/src/database/schema/accounting.prisma` — `FiscalPeriod` model (`00_BUSINESS_RULES.md` Ch.6: the monthly-typical subdivision of a Financial Year — `startDate`/`endDate`, `FiscalPeriodStatus` lifecycle Open/SoftClosed/Closed/Reopened per Ch.6.5). Tenant-owned (`tenantId`, MT-001); carries `companyUuid` directly (cross-module reference, FK-002, no DB-level FK) even though reachable via its parent Financial Year, mirroring Organization's Branch/Department carrying both `tenantId` and `companyId` alongside their real parent FK. `financialYearId` is a real, in-module RESTRICT FK to `FinancialYear.id` (FK-001/FK-003/FK-004) since both models live in the same module schema file — cross-module prohibition (FK-002) does not apply here. `uq_fiscal_periods_financial_year_start_date_deleted_at` gives partial duplicate-start-date protection within a Financial Year (SD-004); true overlap validation and chronological-close ordering (FP-002) are explicitly deferred to a later Business-layer milestone. No `periodNumber`/`periodName` field was added — the handbook (Ch.6.1-6.20) does not define one, and ordering is derivable from `startDate`; flagged here rather than invented. Migration `20260804162814_accounting_create_fiscal_period` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, `SHOW CREATE TABLE fiscal_periods` and `_prisma_migrations` confirm the table, FK constraint (RESTRICT), tenant-leading composite index, and FK-002 uuid-reference pattern per `06_DATABASE_STANDARDS.md`. `npm run typecheck` and `npm run lint` (15 modules scanned) both pass. Repository/Business/Presentation/Integration/API-Testing/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Fiscal Period — Repository Layer. `apps/api/src/shared/accounting/domain/{aggregates/fiscal-period.aggregate.ts, enums/fiscal-period-status.enum.ts}` (new), `domain/errors/accounting.errors.ts` extended with `FiscalPeriodNotFoundError`/`InvalidFiscalPeriodStatusTransitionError`, `domain/interfaces/accounting-repository.interface.ts` extended with 8 Fiscal Period methods, `repository/accounting.repository.ts` extended with their `PrismaAccountingRepository` implementations — mirroring Financial Year's/Organization's/User Management's/Authorization's exact repository pattern. `FiscalPeriod` aggregate exposes only `softClose()` (Open→SoftClosed), `close()` (SoftClosed/Reopened→Closed), and `reopen()` (Closed→Reopened) — the three transitions Ch.6.5's lifecycle diagram actually documents; deliberately **no aggregate `open()` method**, since the diagram shows no edge into Open other than initial creation (the Database layer already defaults new rows to `OPEN`), unlike FinancialYear's Future→Open edge. The Repository nonetheless implements `openFiscalPeriod` (a raw status-setter, no `from`-state check) per explicit instruction, mirroring how `openFinancialYear`/`closeFinancialYear`/`reopenFinancialYear` already never call their own aggregate's transition methods either — flagged as a documented deviation, not a guess. `listFiscalPeriods` takes an optional `financialYearId` narrowing param, mirroring `listFinancialYears`' optional `companyUuid` param. `financialYearId` is a real in-module FK (RESTRICT), accepted as a plain `bigint` with no cross-repository existence check (that's a future Business-layer concern); `companyUuid` remains a cross-module FK-002 reference, never validated. Every method re-asserts `tenantId` explicitly (MT-002); updates use the `updateMany` + refetch pattern, throwing `FiscalPeriodNotFoundError` on zero rows. Test-support fixture (`business/test-support/fixtures.ts`) mechanically extended with 8 new `jest.fn()` mocks to keep `IAccountingRepository`'s type satisfied — no other existing code touched or refactored. Verified via `prisma validate`/`generate` (schema unchanged, no new migration needed — Database layer was already frozen), `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), `npm test -w apps/api` (443 tests, 78 suites, no regressions), and a temporary script exercising every new repository method end-to-end against local Homebrew MySQL (create/find/list/update, open→softClose→close→reopen→open cycle, not-found error path, cross-tenant isolation on both reads and mutations) — script and all test data (a throwaway `FinancialYear` + its `FiscalPeriod` rows under tenants 900001/900002) deleted after the run, confirmed via direct query. Business/Presentation/Integration/API-Testing/Frontend layers intentionally not started — stop condition for this milestone.

**Completed (continued):** Fiscal Period — Business Layer. `apps/api/src/shared/accounting/business/{create,get,update,list,soft-close,close,reopen}-fiscal-period.service.ts` + `validate-fiscal-period-open.service.ts` (8 use cases), plus specs for each, mirroring Financial Year's/Organization's/Authorization's exact use-case-per-file pattern — every service depends only on `IAccountingRepository`, no Prisma/Express/Zod. `createFiscalPeriod`/`listFiscalPeriods` accept an external `financialYearUuid` (not the internal `financialYearId`, PK-003) and resolve it via `repository.findFinancialYearByUuid` first, mirroring the documented "resolve parent first" pattern from Authorization's `assignPermission`/Organization's `createBranch`; `createFiscalPeriod` also derives `companyUuid` from the resolved parent Financial Year rather than accepting it as a separate input, so a Fiscal Period's Company can never drift from its own Financial Year's. Business rules implemented: (1) overlap validation — a new/revised Fiscal Period's date range must not overlap another Fiscal Period already registered within the same Financial Year, checked in both `createFiscalPeriod` and `updateFiscalPeriod` (exclude-self on update), via new `FiscalPeriodOverlapError`; (2) "belongs to exactly one Financial Year" — enforced by requiring and resolving `financialYearUuid` in `createFiscalPeriod`, throwing `FinancialYearNotFoundError` if it doesn't exist; (3) a Closed Fiscal Period cannot be modified — `updateFiscalPeriod` checks this before anything else, via new `FiscalPeriodClosedError`; (4) only Open periods allow posting — `validateFiscalPeriodOpen`, mirroring `validateFinancialYearOpen`, via new `FiscalPeriodNotOpenError`; (5) `validateFiscalPeriodOpen` has no controller (internal guard only, per instruction); (6) lifecycle transitions enforced via the `FiscalPeriod` aggregate's `softClose()`/`close()`/`reopen()` (added in the Repository milestone) before persisting — `InvalidFiscalPeriodStatusTransitionError` on illegal transitions. No `openFiscalPeriod` Business service was created — not in the requested list of 8, and consistent with the Repository milestone's finding that Ch.6.5 documents no transition into Open besides initial creation; the Repository's raw `openFiscalPeriod` persistence method remains unused by this layer, same as before. `domain/errors/accounting.errors.ts` extended with the three new error classes above (needed, not speculative); `business/test-support/fixtures.ts` extended with a `buildFiscalPeriod` builder mirroring `buildFinancialYear`. No existing Financial Year business files modified beyond the mechanical fixture addition from the prior Repository milestone. Verified via `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), and `npm test -w apps/api` (**477 tests, 86 suites** — 34 new tests across 8 new spec files, no regressions). Presentation/Integration/API-Testing/Frontend layers intentionally not started — stop condition for this milestone.

**Completed (continued):** Fiscal Period — Presentation Layer. `apps/api/src/shared/accounting/presentation/` extended with 7 controllers (`create`/`get`/`update`/`list-fiscal-periods`, `soft-close`/`close`/`reopen-fiscal-period`), 4 request DTOs (`fiscal-period-uuid.schema.ts`, `create-fiscal-period.dto.ts`, `update-fiscal-period.dto.ts`, `list-fiscal-periods-query.dto.ts` — `tenant-id-header.schema.ts` reused as-is, module-wide not per-entity), and `fiscal-period.response.dto.ts`, mirroring Financial Year's exact pattern. `index.ts` mounts all 7 routes under `/api/v1/accounting` (already-shared mount, no `module-registry.ts` wiring change needed beyond a documentation-comment update noting Fiscal Period is now covered). Endpoints: `POST/GET /fiscal-periods`, `GET/PUT /fiscal-periods/:fiscalPeriodUuid`, `POST /fiscal-periods/:fiscalPeriodUuid/{soft-close,close,reopen}` — `validateFiscalPeriodOpen` deliberately has no endpoint (internal guard only, mirroring `validateFinancialYearOpen`), and there is no `open` endpoint either (Ch.6.5's diagram has no transition into Open besides initial creation, confirmed across the Repository/Business milestones). `createFiscalPeriodRequestSchema` accepts `financialYearUuid` (not `companyUuid` — the Business layer derives it from the resolved parent Financial Year). Response DTO exposes only `uuid`/`companyUuid`/`startDate`/`endDate`/`status`/`createdAt`/`updatedAt` — never `id`/`tenantId`/`createdBy`/`updatedBy`/`deletedAt`, and also never `financialYearId` (an internal in-module FK id, PK-003 applies to it too; the aggregate holds no `financialYearUuid` to expose instead, and resolving one would cost an extra Financial Year lookup per row — flagged, not silently dropped). Error codes added to the module's shared `domain-error-http-mapping.ts`: `ACC_FISCAL_PERIOD_NOT_FOUND` (404), `ACC_INVALID_FISCAL_PERIOD_STATUS_TRANSITION` (409), `ACC_FISCAL_PERIOD_OVERLAP` (409), `ACC_FISCAL_PERIOD_CLOSED` (409), `ACC_FISCAL_PERIOD_NOT_OPEN` (403), default `ACC_DOMAIN_ERROR` (422) — `response-envelope.ts`/`handle-domain-errors.ts` needed no changes (already generic). `business/accounting-types.ts` extended with a `FiscalPeriodStatus` re-export (same Presentation seam `FinancialYearStatus` already used). Verified via `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), and `npm test -w apps/api` (**501 tests, 86 suites** — 24 new integration tests added to the existing `test/integration/accounting/accounting.routes.spec.ts`, no regressions), covering success, header/body/query validation, not-found, overlap (create + update), closed-blocks-modification, every legal/illegal lifecycle transition, and response envelope/field-exposure shape. Application Integration/API-Testing/Frontend intentionally not started — stop condition for this milestone.

**Completed (continued):** Fiscal Period — Application Integration & API Testing. Verified (no code changes needed — wiring from the Presentation milestone was already complete and correct): `shared/accounting/index.ts` mounts all 7 Fiscal Period routes on the same `createAccountingRouter`/`createDefaultAccountingRouter` Financial Year already uses; `accounting.composition.ts` constructs exactly one `PrismaAccountingRepository`, shared by both entities; `module-registry.ts` mounts Accounting once (`jwtAuthMiddleware` + `createCurrentTenantMiddleware({rewriteHeaderAs:"decimal"})` ahead of it, updated only a stale doc comment to say "Financial Year, Fiscal Period"); `server.ts` needs no per-module change (calls `registerModules(app)` generically; correlation-id/logging mounted globally ahead of it, `errorHandlerMiddleware` globally after). Confirmed via grep: only `repository/accounting.repository.ts` imports Prisma/the generated client anywhere in the module; no `business/` file imports Express/Zod/Prisma; no `presentation/` file imports `domain/` directly; `createPermissionMiddleware` remains unmounted anywhere in the app (grepped — only its own file references itself). All 7 routes confirmed reachable live against the real running app (`tsx src/server.ts`) via the new Postman collection (see below). Re-verified `/health` (200), unauthenticated `GET /api/v1/users` and `GET /api/v1/authorization/roles` (401, confirming jwt-auth still gates other modules), and a full re-run of `FinancialYear.postman_collection.json` (still 25/25 requests, 107/107 assertions, 0 failures — confirming Fiscal Period's wiring didn't disturb Financial Year's). `npm run typecheck`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` all pass (501 tests, 86 suites, no regressions this milestone touched no application code).

**Completed (continued):** Fiscal Period — API Testing. `engineering/testing/accounting/FiscalPeriod.postman_collection.json` (new file; `LedgerOne.postman_environment.json` reused as-is, not modified — same environment already shared with Financial Year's collection). One folder ("Fiscal Period"), 27 requests: a setup step (create the parent Financial Year fixture), then create/get/list/update/soft-close/close/reopen success paths, 4 validation failures (malformed financialYearUuid on create/get/list query, missing required fields), 1×401, 6×404, 2×409-overlap (create + update), 4×409-invalid-transition, 1×409-closed-period-update, list with and without `?financialYearUuid=` filter, and per-response negative assertions that `id`/`tenantId`/`financialYearId`/`createdBy`/`updatedBy`/`deletedAt` never appear. A second, independent Fiscal Period (June) is created purely to exercise overlap/invalid-transition paths without disturbing the primary Fiscal Period's own lifecycle sequence — same pattern as Financial Year's own collection. Distinct date-window formula (base 3000, not Financial Year's own base 2030) in the collection-level pre-request script, so this collection's own Financial Year fixture never collides with `FinancialYear.postman_collection.json`'s separately-seeded rows under the same `companyUuid` in the same `financial_years` table. Verified via two consecutive `npx newman run` invocations against the real running app and local MySQL: **27/27 requests, 114/114 assertions, 0 failures both times**, each run landing on a different generated date window, confirming true repeatability. Test data (4 Financial Years + their Fiscal Periods across the 3 total Newman runs this session, all under tenant 900001) cleaned up afterward via direct `DELETE FROM fiscal_periods`/`DELETE FROM financial_years`; confirmed zero rows remain; server stopped. Frontend/Currency/Tax/Chart of Accounts/Journal Entries remain out of scope.

**Completed (continued):** Currency — Database Layer + Repository Layer. `apps/api/src/database/schema/accounting.prisma` extended with `Currency` (00_BUSINESS_RULES.md Ch.7: ISO code, name, symbol, decimal-precision convention per CUR-003) and `ExchangeRate` (Ch.31: currency pair, rate value, effective date per EXR-001/002) models, plus a `CurrencyStatus` enum (Active/Inactive, Ch.7.5/7.8). Currency is **platform-owned reference data** (Ch.7.5 — "largely static... not created or deleted by end users in the ordinary course of business"), mirroring Authorization's Permission (MT-005): no `tenant_id`, no `created_by`/`updated_by` (06_DATABASE_STANDARDS.md Ch.2.3's documented platform-reference-table exception), `isoCode` unique with no `deletedAt` composite (a deliberate never-reuse decision for ISO 4217 codes, flagged inline rather than assumed). Exchange Rate is **tenant-owned** (`tenant_id`, MT-001) — rates are "entered" per Ch.31.5 for a tenant's own operations, the same posture as Financial Year/Fiscal Period; `fromCurrencyId`/`toCurrencyId` are real, in-module RESTRICT FKs to `Currency.id` (FK-001/FK-003/FK-004, both models share this schema file); `rate` is `DECIMAL(20,10)` (no handbook-documented precision/scale exists for rate values — flagged as a Handbook Deviation, decided inline, wider than a currency-precision amount since a rate is a ratio). Migration `20260804171755_accounting_create_currency_exchange_rate` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, `SHOW CREATE TABLE` confirms both tables, dual keys, soft delete, the platform-vs-tenant column split, and FK/index rules per `06_DATABASE_STANDARDS.md`. Repository layer: `apps/api/src/shared/accounting/domain/{aggregates/currency.aggregate.ts, entities/exchange-rate.entity.ts, enums/currency-status.enum.ts}` (new), `domain/errors/accounting.errors.ts` extended with `CurrencyNotFoundError`/`InvalidCurrencyStatusTransitionError`, `domain/interfaces/accounting-repository.interface.ts` and `repository/accounting.repository.ts` extended with 7 Currency methods (`createCurrency`, `findCurrencyByUuid`, `findCurrencyByIsoCode`, `listCurrencies` optionally filtered by status, `updateCurrency`, `activateCurrency`/`deactivateCurrency` as raw persistence transitions) and 3 Exchange Rate methods (`createExchangeRate`, `findExchangeRateByUuid`, `listExchangeRates` optionally filtered by currency pair) — mirroring Financial Year's/Fiscal Period's exact repository pattern (`updateMany`+refetch, explicit `tenantId` re-assertion where tenant-owned, `RepositoryTransaction` support on every mutation, no Prisma leakage). `Currency` is an aggregate (`activate()`/`deactivate()` transition methods, mirroring FinancialYear/FiscalPeriod); `ExchangeRate` is a plain entity with no lifecycle methods (Ch.31.5/EXR-002 — an immutable historical time series once created) and deliberately has **no update/remove repository method** — a correction is a new, dated rate row, a future Business-layer concern, mirroring Authorization's RolePermission/UserRole (created or removed, never partially updated). `business/test-support/fixtures.ts` mechanically extended with 10 new `jest.fn()` mocks to keep `IAccountingRepository`'s type satisfied — no other existing business file touched. Verified via `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), `npm test -w apps/api` (501 tests, 86 suites, no regressions), and a temporary script exercising every new repository method end-to-end against local MySQL (Currency CRUD, activate/deactivate lifecycle, status-filtered listing, not-found error path; Exchange Rate create/find/list, exact-decimal-precision round-trip, currency-pair filtering, cross-tenant isolation on both reads and mutations) — script and all test data (2 throwaway Currencies + 2 Exchange Rates under tenants 900901/900902) deleted after the run, confirmed via direct query (0 rows remain). **Handbook Deviations flagged (not implemented, not guessed):** (1) a "Company activates Currency" join/activation table — Ch.7.8/7.10 describe the relationship but no chapter names or schemas the join entity itself; (2) no `isBase`/base-currency flag on Currency — CMP-002 places that fact on Company, a future Organization-schema concern; (3) `ExchangeRate.rate`'s `DECIMAL(20,10)` precision/scale — no handbook chapter specifies one; (4) rounding *algorithm* (round-half-up vs banker's) for CUR-003 — never specified, not invented. Business/Presentation/Integration/API-Testing/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Foundation Correction — `DecimalValue` Domain Value Object. `apps/api/src/shared/accounting/domain/value-objects/decimal-value.value-object.ts` (new) — LedgerOne's first Domain Value Object (03_ARCHITECTURE.md Ch.7.3.2/7.3.6), replacing the Currency Repository milestone's `ExchangeRate.rate: string` mapping (a flagged review finding: a raw string satisfied "don't leak Prisma's `Decimal`" but discarded the invariant-encapsulation a Value Object exists to provide). Immutable, ORM/framework-independent (no Prisma/Express/Zod imports), constructed via a named static factory (`DecimalValue.create`, 05_CODING_STANDARDS.md Ch.15.5) that validates a well-formed decimal string and normalizes it (trailing/leading zeros, negative zero) so equal values compare equal; exposes only `toString()`/`equals()`/`isPositive()` — deliberately no arithmetic, `compare()`/ordering, or currency awareness (none justified by current usage; `isPositive()` was added because Ch.31.8 explicitly requires it and this milestone's `createExchangeRate` is its first real caller). No third-party arbitrary-precision math library used (none in the frozen stack, 02_TECH_STACK.md). `ExchangeRate.rate` and `CreateExchangeRateProps.rate` updated from `string` to `DecimalValue`; `repository/accounting.repository.ts`'s mapping now goes Prisma `Decimal` → `.toFixed()` (never `.toString()`, to avoid exponential notation) → `DecimalValue.create(...)`, and back to a plain string via `.toString()` for Prisma's create input. Verified via `prisma validate`/`generate` (no schema change), `npm run typecheck`/`npm run lint`/`npm test -w apps/api` (501 tests, 86 suites, no regressions), and a temporary script round-tripping exact precision through the real `DECIMAL(20,10)` column — script and test data deleted afterward.

**Completed (continued):** Currency — Business Layer + Presentation Layer. Business: `apps/api/src/shared/accounting/business/{create,get,update,activate,deactivate,list}-currency.service.ts` (6 use cases) and `{create,get,list}-exchange-rate.service.ts` (3 use cases), every one depending only on `IAccountingRepository` — no Prisma/Express/Zod. `createCurrency` checks `findCurrencyByIsoCode` first, throwing new `DuplicateCurrencyIsoCodeError` on a match (Repository performs no duplicate checking of its own). `activate`/`deactivateCurrency` call the `Currency` aggregate's `activate()`/`deactivate()` (validating the `from` state) before persisting, mirroring `open/close/reopen-financial-year.service.ts` exactly. `createExchangeRate` resolves both `fromCurrencyUuid`/`toCurrencyUuid` to their Currency rows first (mirroring `create-fiscal-period.service.ts`'s parent-resolution pattern) and enforces every Ch.31.8 Validation Rule: the pair must be two distinct Currencies (new `ExchangeRateCurrencyPairNotDistinctError`), both must be Active (new `CurrencyNotActiveError`), and the rate must be positive (new `InvalidExchangeRateValueError`, via `DecimalValue.isPositive()`) — plus a new `DuplicateExchangeRateError` guarding the Database milestone's own `uq_exchange_rates_tenant_pair_effective_date_deleted_at` constraint so a duplicate attempt never leaks a raw Prisma error (Handbook Deviation — not explicit Ch.31 text, a direct consequence of that schema decision). New `ExchangeRateNotFoundError` for `getExchangeRate`. `accounting-types.ts` extended with `CurrencyStatus`/`DecimalValue` re-exports (the Presentation-facing seam). Presentation: `presentation/{controllers/v1,dto/requests,dto/responses}` extended with 6 Currency endpoints (`create`/`get`/`update`/`list`, `activate`/`deactivate`) mounted with **no `X-Tenant-Id` header** (Currency is platform-owned reference data, Ch.7.5/MT-005, mirroring Authorization's `/permissions`) and 3 Exchange Rate endpoints (`create`/`get`/`list`) mounted **with** the header (tenant-owned, MT-001) — no update/remove Exchange Rate endpoint (immutable time series, Ch.31.5/EXR-002). Currency's response DTO exposes `uuid`/`isoCode`/`name`/`symbol`/`decimalPrecision`/`status`/timestamps only. Exchange Rate's response DTO deliberately omits `fromCurrencyId`/`toCurrencyId` entirely rather than resolving them to Currency `uuid`s — mirroring Fiscal Period's own documented choice to omit `financialYearId` rather than pay an extra lookup per row (flagged, not silently dropped). `createCurrencyRequestSchema`'s `isoCode` format (`^[A-Z]{3}$`) is inferred from Ch.7.1's ISO 4217 examples plus the `VARCHAR(3)` column width, not literal handbook text (Handbook Deviation). `createExchangeRateRequestSchema`'s `rate` is validated only for non-empty shape (`min(1)`) — the actual decimal-format/positivity invariants are enforced once, by `DecimalValue.create`/`isPositive` (Ch.15.4), not duplicated as a second regex. 9 new `ACC_*` error codes added to `domain-error-http-mapping.ts` (`ACC_CURRENCY_NOT_FOUND` 404, `ACC_INVALID_CURRENCY_STATUS_TRANSITION` 409, `ACC_DUPLICATE_CURRENCY_ISO_CODE` 409, `ACC_CURRENCY_NOT_ACTIVE` 403, `ACC_EXCHANGE_RATE_NOT_FOUND` 404, `ACC_EXCHANGE_RATE_PAIR_NOT_DISTINCT` 422, `ACC_INVALID_EXCHANGE_RATE_VALUE` 422, `ACC_DUPLICATE_EXCHANGE_RATE` 409, `ACC_INVALID_DECIMAL_VALUE` 422). Routes mounted on the same shared `createAccountingRouter`/`createDefaultAccountingRouter` Financial Year/Fiscal Period already use — no new module, no `module-registry.ts` change needed. Verified via `npm run typecheck`/`npm run lint` (15 modules scanned)/`npm test -w apps/api` (**554 tests, 95 suites** — 34 new business-layer tests across 9 spec files plus 25 new integration tests in the existing `accounting.routes.spec.ts`, no regressions), `prisma validate`/`generate` (schema unchanged), and explicit greps confirming no Prisma import outside `repository/`, no `presentation/` file importing `domain/` directly. Application Integration/API-Testing/Frontend intentionally not started — stop condition for this milestone, per explicit instruction. Chart of Accounts, Journal Entries, and General Ledger were not started, per explicit instruction.

**Completed (continued):** Currency — Application Integration & API Testing. Verified (no code changes needed beyond one test-collection bug fix — the wiring from the Business/Presentation milestone was already complete and correct): `shared/accounting/index.ts` mounts all 9 Currency/Exchange Rate routes on the same `createAccountingRouter`/`createDefaultAccountingRouter` Financial Year/Fiscal Period already use; `accounting.composition.ts` constructs exactly one `PrismaAccountingRepository`, shared by all three entities; `module-registry.ts` mounts Accounting once (`jwtAuthMiddleware` + `createCurrentTenantMiddleware({rewriteHeaderAs:"decimal"})` ahead of it — no code change needed, no doc-comment update made since the mount is generic and already correct for the whole module). Confirmed via grep: only `repository/accounting.repository.ts` imports Prisma/the generated client anywhere in the module; no `presentation/` file imports `domain/` directly. **Live finding (confirmed empirically, not assumed):** Currency's "platform-owned, no `X-Tenant-Id`" design does NOT mean unauthenticated — every `/api/v1/accounting/*` route, Currency included, still sits behind `jwtAuthMiddleware` (a request with no `Authorization` header 401s with `AUTH_MISSING_TOKEN` even on Currency endpoints); confirmed live via `tsx src/server.ts` against local MySQL. All 9 endpoints exercised end-to-end live: Currency create/duplicate-ISO(409)/get/get-not-found(404)/get-malformed-uuid(422)/update/update-not-found(404)/deactivate/deactivate-again-invalid-transition(409)/list/list-filtered-by-status; Exchange Rate create-unauthorized(401)/create-malformed-rate(`ACC_INVALID_DECIMAL_VALUE`,422)/create-currency-not-found(404)/create-invalid-pair(422)/create-inactive-currency(403)/create-non-positive-rate(422)/create-success(exact `DecimalValue` precision round-trip through `DECIMAL(20,10)` confirmed)/create-duplicate(409)/get/get-not-found(404)/list/list-filtered; confirmed live that no PUT/PATCH/DELETE route exists for Exchange Rate at all (a genuine Express-level 404, not a domain error — historical immutability, Ch.31.5/EXR-002, enforced structurally) and no DELETE route exists for Currency (Ch.7.5). Re-verified `/health`, unauthenticated `GET /api/v1/users`/`GET /api/v1/authorization/roles` (401), and `GET /api/v1/accounting/financial-years`/`fiscal-periods` (still 200, unaffected) against the same running app. `engineering/testing/accounting/Currency.postman_collection.json` (new; `LedgerOne.postman_environment.json` reused as-is, no changes needed) — one collection, "Currency"/"Exchange Rate"/"Currency Lifecycle Cleanup" folders, 34 requests covering every endpoint's success/validation/duplicate/lifecycle/not-found/inactive-currency/invalid-pair paths plus per-response negative assertions that `id`/`tenantId`/`fromCurrencyId`/`toCurrencyId`/`createdBy`/`updatedBy`/`deletedAt` never appear. Repeatability solved with a collection-level pre-request script generating three ISO codes from the current timestamp across the full `^[A-Z]{3}$` namespace (26³ = 17576 combinations, base26-encoded) — Currency's global ISO-code uniqueness with no delete endpoint at all (Ch.7.5) ruled out Financial Year's/Fiscal Period's own moving-date-window trick, since there's no API-level way to remove a Currency between runs. One bug found and fixed during the first Newman run: the shared collection-level "response is a `{data}`/`{error}` envelope" test unconditionally called `pm.response.json()`, which threw on the deliberate "no update endpoint" 404 (a genuine Express HTML error page, not this module's JSON envelope) — fixed by gating both shared assertions on the response's actual `Content-Type` header. Verified via two consecutive `npx newman run` invocations against the real running app and local MySQL: **34/34 requests, 136/136 assertions, 0 failures on the second run** (first run had the one envelope-test bug above, fixed before the second run — both runs' actual endpoint behavior was correct throughout), each run landing on a distinct generated ISO-code triplet (confirmed `BSO/BSP/BSQ` vs `XKE/XKF/XKG` in the database), confirming true repeatability. `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` re-confirmed unaffected throughout (554 tests, 95 suites — this milestone touched no application source beyond the Postman collection). Test data (6 Currencies + 2 Exchange Rates across both Newman runs, plus manual live-verification rows, all under tenant 900001) cleaned up afterward via direct `DELETE FROM exchange_rates`/`DELETE FROM currencies`; confirmed zero rows remain; server stopped; all temporary log files deleted. No handbook deviations beyond those already flagged in the Business/Presentation milestone.

**Completed (continued):** Tax — Database Layer + Repository Layer. `apps/api/src/database/schema/accounting.prisma` extended with `TaxGroup` (00_BUSINESS_RULES.md Ch.67: named tax classification — name only) and `TaxRule` (Ch.68: the rate/effective-date-range calculation logic a Tax Group maps to). Only these two entities were modeled — GST (Ch.66.5), Reverse Charge (Ch.69.5), and Input Tax Credit (Ch.70.5) are each explicitly documented in their own chapters as "not an independent entity — a calculation/posting behavior," not a persisted table, so no tables were invented for them (Handbook Deviation, flagged not guessed). `TaxGroup` is tenant-owned (`tenantId`, MT-001) and carries `companyUuid` directly (cross-module reference, FK-002, no DB-level FK) — mirroring FinancialYear's own ownership shape, inferred from Ch.67.13's "Finance Manager or Company Administrator approval" (Company-level configuration, not platform-wide reference data like Currency). No status/lifecycle column: Ch.67.5 documents Tax Group as "static, low-change reference data" with no state machine. `TaxRule.taxGroupId` is a real, in-module RESTRICT FK (FK-001/FK-003/FK-004, both models share this schema file); `rate` is `DECIMAL(9,4)` (no handbook-documented precision/scale for a tax percentage — Handbook Deviation, mirroring ExchangeRate.rate's own documented precision deviation, sized for a percentage rather than ExchangeRate's wider ratio-oriented `DECIMAL(20,10)`); `effectiveFrom`/nullable `effectiveTo` model TXR-001's "effective date range" (TXR-001's non-overlap validation and TXR-002's date-of-transaction resolution are explicitly deferred to a future Business-layer milestone, mirroring Financial Year/Fiscal Period's own deferred overlap validation). Tax Rule's jurisdiction-variation (Ch.68.3/68.10 — "potentially varying by Business Location") was deliberately not modeled as a `businessLocationUuid` reference: Business Location (Ch.9) has no table anywhere in the codebase yet (Organization's schema defines only Tenant/Company/Branch/Department) — referencing a nonexistent table would be inventing a shape the handbook never schemas itself (Handbook Deviation, flagged not guessed). Tax Rule's "calculation basis" (Ch.68.3) is modeled as a single percentage rate only — every worked example across Ch.66/67/68 is percentage-of-transaction-amount; no second basis type is ever concretely defined, so no `basis` enum was added speculatively. Migration `20260804184216_accounting_create_tax_group_tax_rule` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, generated SQL confirms dual keys, soft delete, tenant-leading composite indexes, the in-module `RESTRICT` FK, and the FK-002 `companyUuid` pattern per `06_DATABASE_STANDARDS.md`. Repository layer: `apps/api/src/shared/accounting/domain/entities/{tax-group,tax-rule}.entity.ts` (new — plain entities, not aggregates, since neither chapter documents a lifecycle state machine; `TaxGroup` supports revision per Ch.67.5's "updated... in response to regulatory change", `TaxRule` is immutable per TXR-003's "a rate correction requires a new, dated rule," mirroring ExchangeRate's own immutability), `domain/errors/accounting.errors.ts` extended with `TaxGroupNotFoundError` only (no `TaxRuleNotFoundError` — Tax Rule has no update method to throw one from; `findTaxRuleByUuid` returns `null`, mirroring ExchangeRate's find-only not-found handling), `domain/interfaces/accounting-repository.interface.ts` and `repository/accounting.repository.ts` extended with 4 Tax Group methods (`createTaxGroup`, `findTaxGroupByUuid`, `listTaxGroups` optionally filtered by `companyUuid`, `updateTaxGroup`) and 3 Tax Rule methods (`createTaxRule`, `findTaxRuleByUuid`, `listTaxRules` optionally filtered by `taxGroupId`) — mirroring Currency's/ExchangeRate's exact repository pattern (`updateMany`+refetch, explicit `tenantId` re-assertion, `RepositoryTransaction` support on every mutation, `DecimalValue` round-trip for `rate` via `.toFixed()`, no Prisma leakage). `business/test-support/fixtures.ts` mechanically extended with `buildTaxGroup`/`buildTaxRule` builders and 7 new `jest.fn()` mocks to keep `IAccountingRepository`'s type satisfied — no other existing business file touched. Verified via `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), `npm test -w apps/api` (554 tests, 95 suites, no regressions — this milestone added no business-layer tests, consistent with a Repository-only stop condition), and a temporary script exercising every new repository method end-to-end against local MySQL (Tax Group create/find/list/update, not-found error path; Tax Rule create/find/list, exact-decimal-precision round-trip through `DECIMAL(9,4)`, tax-group-filtered and tenant-wide listing, `$transaction` support, cross-tenant isolation on both reads and mutations) — script and all test data (tenants 900910/900920) deleted after the run, confirmed via direct count query (0 rows remain). Business/Presentation/Integration/API-Testing/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Tax — Business Layer + Presentation Layer. Business: `apps/api/src/shared/accounting/business/{create,get,update,list}-tax-group.service.ts` (4 use cases) and `{create,get,list}-tax-rule.service.ts` (3 use cases), every one depending only on `IAccountingRepository` — no Prisma/Express/Zod, per explicit instruction; no service for GST/Reverse Charge/Input Tax Credit calculation, CGST/SGST split, Journal Entries, or Product tax resolution (all explicitly out of scope, belonging to future modules per Ch.66/69/70's own "not an independent entity" framing and the Product/Invoice/Journal Entry chapters). `createTaxGroup` checks `listTaxGroups` for a same-name match within the Company first, throwing new `DuplicateTaxGroupNameError` (Repository performs no duplicate checking of its own) — not explicit Ch.67 text, a direct consequence of the Database milestone's own `uq_tax_groups_tenant_company_name_deleted_at` constraint, mirroring `create-exchange-rate.service.ts`'s identical `DuplicateExchangeRateError` reasoning; `updateTaxGroup` re-checks the same rule, excluding the row being revised. `createTaxRule` resolves its parent Tax Group by `taxGroupUuid` first (mirroring `create-fiscal-period.service.ts`'s parent-resolution pattern), then enforces Ch.68.8's Validation Rules: the rate must be **non-negative** (new `InvalidTaxRateValueError`, via a new `DecimalValue.isNegative()` — deliberately not `isPositive()`, since Ch.67.11's "Zero Rate" Tax Group example makes an exact 0% rate explicitly valid, unlike Exchange Rate's stricter positive-only rule) and the effective date range must not overlap another Tax Rule already registered for the same Tax Group (Ch.68.7 TXR-001, new `TaxRuleOverlapError`; a `null`/omitted `effectiveTo` is treated as open-ended/unbounded for the overlap comparison). A supplied `effectiveTo` earlier than `effectiveFrom` is rejected by new `InvalidTaxRuleEffectiveDateRangeError` (a documented Handbook Deviation — the minimum structural invariant a "range" implies, not literal Ch.68 text). New `TaxGroupNotFoundError`-triggered paths added to `getTaxGroup`/`updateTaxGroup`/`createTaxRule`/`listTaxRules` (when a filtering `taxGroupUuid` doesn't resolve); new `TaxRuleNotFoundError` added for `getTaxRule` (Tax Rule has no update method — TXR-003 immutability — so this is its only use, mirroring `ExchangeRateNotFoundError`'s identical find-only usage). No `resolveApplicableTaxRule`/"guard" service was added — TXR-002's "use the rule effective on the transaction date" resolution is explicitly a future consuming module's (Sales/Purchase Invoice) concern, not one of the 7 requested services, mirroring how Ch.31.12's analogous Exchange Rate "most recently effective" lookup was likewise left unimplemented at the Business layer. `domain/value-objects/decimal-value.value-object.ts` extended with `isNegative()` (a second real caller beyond `isPositive()`, both now usage-justified, not speculative). `domain/errors/accounting.errors.ts` extended with the 5 new error classes above. Presentation: `presentation/{controllers/v1,dto/requests,dto/responses}` extended with 4 Tax Group endpoints (`create`/`get`/`update`/`list`) and 3 Tax Rule endpoints (`create`/`get`/`list` — no update/remove, immutable per TXR-003) mounted on the existing `createAccountingRouter`/`createDefaultAccountingRouter`, **with** the `X-Tenant-Id` header on every route (tenant-owned, MT-001, unlike Currency's platform-owned no-header posture). Tax Group's response DTO exposes `uuid`/`companyUuid`/`name`/timestamps only. Tax Rule's response DTO deliberately omits `taxGroupId` entirely rather than resolving it to the Tax Group's `uuid` — mirroring Exchange Rate's own documented choice to omit `fromCurrencyId`/`toCurrencyId` rather than pay an extra lookup per row (flagged, not silently dropped). `createTaxRuleRequestSchema`'s `rate` is validated only for non-empty shape (`min(1)`) — the actual decimal-format/non-negativity invariants are enforced once, by `DecimalValue.create`/`isNegative` (Ch.15.4), not duplicated as a second regex, mirroring `createExchangeRateRequestSchema`'s identical reasoning. 6 new `ACC_*` error codes added to `domain-error-http-mapping.ts` (`ACC_TAX_GROUP_NOT_FOUND` 404, `ACC_DUPLICATE_TAX_GROUP_NAME` 409, `ACC_TAX_RULE_NOT_FOUND` 404, `ACC_TAX_RULE_OVERLAP` 409, `ACC_INVALID_TAX_RULE_EFFECTIVE_DATE_RANGE` 422, `ACC_INVALID_TAX_RATE_VALUE` 422). `business/test-support/fixtures.ts`'s pre-existing `buildTaxGroup`/`buildTaxRule` builders (added during the Repository milestone) reused as-is by every new spec. Verified via `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), `npm test -w apps/api` (**606 tests, 102 suites** — 24 new business-layer tests across 7 spec files plus 28 new integration tests added to the existing `accounting.routes.spec.ts`, no regressions), `prisma validate`/`generate` (schema unchanged), and explicit greps confirming no Prisma import outside `repository/`, and no `presentation/` file importing `domain/` directly (only `support/handle-domain-errors.ts`, a same-layer file, is imported — matched by the grep's substring but not an actual `domain/` import). Application Integration/API-Testing/Frontend intentionally not started — stop condition for this milestone, per explicit instruction. Chart of Accounts, Journal Entries, Products, Product Categories, Sales, and Purchases were not started, per explicit instruction.

**Completed (continued):** Tax — Application Integration & API Testing. Verified (no code changes needed — the wiring from the Business/Presentation milestone was already complete and correct): `shared/accounting/index.ts` mounts all 7 Tax Group/Tax Rule routes on the same `createAccountingRouter`/`createDefaultAccountingRouter` Financial Year/Fiscal Period/Currency/Exchange Rate already use; `accounting.composition.ts` constructs exactly one `PrismaAccountingRepository`, confirmed the only construction site anywhere in `apps/api/src`/`test`; `module-registry.ts` mounts Accounting once (`jwtAuthMiddleware` + `createCurrentTenantMiddleware({rewriteHeaderAs:"decimal"})` ahead of it — no code change needed). Confirmed via grep: no Prisma/generated-client import anywhere in the module outside `repository/accounting.repository.ts`; no `presentation/` file imports `domain/` directly; no `business/` file imports `express`/`zod`/Prisma; `createPermissionMiddleware` remains unmounted anywhere in the app. All 7 Tax endpoints exercised end-to-end live against the real running app (`tsx src/server.ts`, port 4000 matching the shared Postman environment) and local Homebrew MySQL: Tax Group create/duplicate-name(409)/get/get-not-found(404)/get-malformed-uuid(422)/update/update-not-found(404)/update-duplicate-name(409)/list/list-filtered-by-companyUuid; Tax Rule create-success(precision round-trip `"18.5000"`→`"18.5"` confirmed)/create-overlap(409)/create-negative-rate(422)/create-zero-rate-success(201, Ch.67.11's "Zero Rate" example)/create-invalid-date-range(422)/create-tax-group-not-found(404)/create-malformed-decimal(`ACC_INVALID_DECIMAL_VALUE`,422)/get/get-not-found(404)/list/list-filtered-by-taxGroupUuid/list-filter-not-found(404); confirmed live that no PUT/DELETE route exists for Tax Rule at all (a genuine Express-level HTML 404, not a domain error — structural immutability per TXR-003, same posture as Exchange Rate) and no DELETE route exists for Tax Group either. Re-verified `/health`, unauthenticated `GET /api/v1/users`/`GET /api/v1/authorization/roles` (401), and `GET /api/v1/accounting/financial-years`/`currencies` (still 200, unaffected) against the same running app. `engineering/testing/accounting/Tax.postman_collection.json` (new; `LedgerOne.postman_environment.json` reused as-is, no changes) — two folders ("Tax Group"/"Tax Rule"), 33 requests covering every endpoint's success/validation/duplicate/overlap/zero-rate/immutability/not-found paths plus per-response negative assertions that `id`/`tenantId`/`taxGroupId`/`createdBy`/`updatedBy`/`deletedAt` never appear. Repeatability solved with a collection-level pre-request script generating a `{{txSuffix}}` (timestamp-derived) for Tax Group names — unique per Company with no delete endpoint to clean up via the API itself — and a distinct `{{txYear}}` date window for Tax Rule effective dates (TXR-001 non-overlap), mirroring Fiscal Period's/Currency's own established per-collection repeatability tricks. Verified via two consecutive `npx newman run` invocations against the real running app and local MySQL: **33/33 requests, 128/128 assertions, 0 failures both times**, each run landing on a distinct generated `txSuffix`/`txYear` (confirmed `...1006904`/`3404` vs `...1016995`/`3495` in the database), confirming true repeatability. `npm run typecheck -w apps/api`, `npm run lint` (15 modules scanned), and `npm test -w apps/api` re-confirmed unaffected throughout (606 tests, 102 suites — this milestone touched no application source beyond the Postman collection). Test data (4 Tax Groups + 4 Tax Rules across both Newman runs, plus manual live-verification rows, all under tenant 900001) cleaned up afterward via direct `DELETE FROM tax_rules`/`DELETE FROM tax_groups`; confirmed zero rows remain; server stopped; no temporary scripts/logs left behind. No handbook deviations beyond those already flagged in the Business/Presentation milestone.

**Completed (continued):** Chart of Accounts — Database Layer + Repository Layer. Preceded by a dedicated, code-free architecture review (Ch.15-33 of 00_BUSINESS_RULES.md plus `03_ARCHITECTURE.md`'s DDD/layering chapters) that surfaced three undefined concepts — Posting vs. Summary Accounts, Contra Accounts, Control Accounts — each resolved by an explicit frozen architectural decision before this milestone began: (1) `Account.isPostingAccount` implements Posting-vs-Summary (only Posting Accounts may receive future Journal Entry postings; Summary Accounts exist only for hierarchy/reporting) — not literal Ch.17 text, a frozen decision now recorded inline in the schema; (2) Contra Accounts and (3) Control Accounts are explicitly out of scope — no flag, sub-type, or reconciliation behavior modeled for either. `apps/api/src/database/schema/accounting.prisma` extended with `AccountGroup` (00_BUSINESS_RULES.md Ch.18: classification category, AGP-001/002/003) and `Account` (Ch.17: the individual ledger-postable classification unit, COA-001-004), plus two shared enums: `AccountType` (Asset/Liability/Equity/Revenue/Expense, Ch.16 DBL-003/Ch.17.7 COA-001) and `AccountStatus` (Draft/Active/Inactive, Ch.17.5). **No separate `ChartOfAccounts` table was modeled** — Ch.17.10's `COMPANY ||--|| CHART_OF_ACCOUNTS: "owns one"` describes a conceptual one-per-Company grouping with no independent fields/rules/lifecycle of its own (everything in Ch.17.3's "Owns:" list describes `Account`'s own fields); a Company's Chart of Accounts is simply its own `Account` rows, mirroring how GST/Reverse Charge/Input Tax Credit were similarly not modeled as tables in the Tax milestone (Handbook Deviation, flagged not guessed). Both models are tenant-owned (`tenantId`, MT-001) and carry `companyUuid` directly (cross-module reference, FK-002, no DB-level FK), mirroring Tax Group's exact ownership shape — inferred from Ch.17.13/18.13's "Company Administrator approval" requirement (Company-level configuration, not platform-wide reference data). `AccountGroup.parentAccountGroupId` and `Account.parentAccountId` are real, self-referential, in-module `RESTRICT` FKs (FK-001/FK-003/FK-004) supporting AGP-003's/COA-002's nesting — AGP-002's type-consistency-with-assigned-accounts and COA-002's type-consistency-throughout-hierarchy are Business-layer concerns (cannot be expressed as a MySQL/Prisma declarative constraint across FK-linked rows), deferred like every other cross-row invariant this module has already deferred (FY-002, TXR-001 overlap). `Account.code` is unique per Company with **no `deletedAt` composite** — Ch.17.7 COA-004 explicitly states codes "cannot be reused for a different account, even after deactivation," making this the first never-reuse unique constraint in the module backed by literal handbook text rather than an inferred hygiene deviation (unlike Currency's ISO code or Tax Group's name). No separate `normalBalance` column — Ch.16 DBL-003 makes it a pure, universal function of `accountType`, never stored independently (a design note, not a deviation). `Account.code`'s `VARCHAR(20)` width and `Account.name`'s `VARCHAR(150)` width are inferred (no handbook-specified length), flagged inline. `AccountType`/`AccountStatus` "platform-provided standard, Company-customizable" language (Ch.17.6/18.5) was read as a future Business-layer seeding workflow, not a database-level platform-vs-tenant ownership split — no separate platform-owned "AccountGroupTemplate" table was modeled, since no chapter schemas one (Handbook Deviation). Migration `20260804193250_accounting_create_account_group_account` applied and verified against local Homebrew MySQL: `prisma validate`/`generate`/`migrate dev` clean, generated SQL confirms dual keys, soft delete, tenant-leading composite indexes, both self-referential `RESTRICT` FKs, the FK-002 `companyUuid` pattern, and the no-`deletedAt` never-reuse unique constraint on `Account.code`. Repository layer: `apps/api/src/shared/accounting/domain/entities/account-group.entity.ts` (new — plain entity, no aggregate, since Ch.18.5 documents no lifecycle state machine, mirroring Tax Group) and `domain/aggregates/account.aggregate.ts` (new — a real aggregate with `activate()`/`deactivate()` transition methods mirroring Currency's/FinancialYear's exact pattern, since Ch.17.5 documents a genuine Draft/Inactive→Active, Active→Inactive state machine), `domain/enums/{account-type,account-status}.enum.ts` (new), `domain/errors/accounting.errors.ts` extended with `AccountGroupNotFoundError`, `AccountNotFoundError`, `InvalidAccountStatusTransitionError`, `domain/interfaces/accounting-repository.interface.ts` and `repository/accounting.repository.ts` extended with 4 Account Group methods (`createAccountGroup`, `findAccountGroupByUuid`, `listAccountGroups` optionally filtered by `companyUuid`, `updateAccountGroup`) and 7 Account methods (`createAccount`, `findAccountByUuid`, `findAccountByCode`, `listAccounts` optionally filtered by `companyUuid`/`accountGroupId`/`status`, `updateAccount`, `activateAccount`, `deactivateAccount`) — mirroring Tax Group's/Currency's exact repository pattern (`updateMany`+refetch, explicit `tenantId` re-assertion, `RepositoryTransaction` support on every mutation, no Prisma leakage, no business/posting/Journal-Entry/Trial-Balance/Financial-Closing validation of any kind). `business/test-support/fixtures.ts` mechanically extended with `buildAccountGroup`/`buildAccount` builders and 11 new `jest.fn()` mocks to keep `IAccountingRepository`'s type satisfied — no other existing business file touched. Verified via `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), `npm test -w apps/api` (606 tests, 102 suites, no regressions — this milestone added no business-layer tests, consistent with a Repository-only stop condition), and a temporary script exercising every new repository method end-to-end against local MySQL (Account Group create/find/list/update including explicit-null parent-clearing, not-found error path; Account create/find-by-uuid/find-by-code/list (company/group/status filters)/update/activate/deactivate lifecycle, default `DRAFT` status and default `isPostingAccount=true` confirmed, an explicit `isPostingAccount=false` Summary Account created and confirmed, duplicate-code rejected by the DB constraint, not-found error path, self-referential parent-hierarchy persistence, `$transaction` support, cross-tenant isolation on both reads and mutations) — script and all test data (tenant 900930) deleted after the run (children deleted before parents to satisfy the self-referential `RESTRICT` FK), confirmed via direct count query (0 rows remain). Business/Presentation/Integration/API-Testing/Frontend layers intentionally not started — stop condition for this milestone, per explicit instruction.

**Completed (continued):** Journal Entries — Database/Repository/Business/Presentation layers (retroactively recorded here — this milestone shipped in commit `463967b "JournalEntries"` without updating this file at the time; recorded now, before the Ledger Read Model milestone below, so this file matches `git HEAD`). Preceded by its own dedicated Aggregate-design architecture review per the "Next up" note this file previously carried. `apps/api/src/database/schema/accounting.prisma` extended with `JournalEntry` (00_BUSINESS_RULES.md Ch.20: `postingDate`, `narration` — a documented Handbook Deviation necessary for manual entry, `status` via a new `JournalEntryStatus` enum Draft/PendingApproval/Posted/Reversed per Ch.20.5, self-referential nullable `reversalOfJournalEntryId` for JRN-003), `JournalEntryLine` (child of `JournalEntry`, real in-module FK to `Account`, separate `debitAmount`/`creditAmount` `DECIMAL(20,4)` columns per `03_ARCHITECTURE.md` Ch.7.3.6's literal `Money` diagram), and `LedgerEntry` (Ch.19: **a separate Aggregate Root from `JournalEntry`**, per the frozen decision that a use case spanning two Aggregates is two separate saves coordinated by the Business layer, `03_ARCHITECTURE.md` Ch.7.6.1 — `journalEntryLineId` unique FK enforcing Ch.19.10's one-to-one "derived from" cardinality, denormalized `debitAmount`/`creditAmount`/`entryDate` mirrored from the source line at posting time, **no `runningBalance` column** (LDG-003 defines balance as a computed sum, never an independently-stored fact), **no `deletedAt`/`updatedAt`/`updatedBy` at all** — the strongest immutability posture in the schema, implementing LDG-002's "never edited or deleted... never deleted at all, soft or hard"). No `fiscalPeriodId` stored on `JournalEntry` — Fiscal Period is resolved from `postingDate` by lookup at validation time, never persisted redundantly. Repository: `IJournalEntryRepository` and `ILedgerRepository`, two more separate interfaces (each Aggregate Root owns its own Repository, per the same Ch.7.6.1 rule) alongside the existing `IAccountingRepository` — `ILedgerRepository.appendLedgerEntry` (not `createLedgerEntry`, naming the append-only semantics explicitly) is the only write method Ledger Entry has at all. Business: `post-journal-entry.service.ts` is the actual LDG-001 implementation — validates DBL-001/002 balance and JRN-002's open-Fiscal-Period requirement, then inside one `$transaction` (via a new `ITransactionRunner`) persists the status transition and appends exactly one `LedgerEntry` per posted `JournalEntryLine`; `reverse-journal-entry.service.ts` implements JRN-003 (a new Draft entry with every line's debit/credit swapped, referencing the original, posted at real "now" via a new `IClock` since the original's date may sit in an already-Closed Fiscal Period, only then marking the original Reversed). Presentation: 8 Journal Entry endpoints (`create`/`get`/`update`/`list`, `submit`/`reject`/`post`/`reverse`) mounted under the existing `/api/v1/accounting` router — **no endpoints for Ledger Entry** at the time (Ch.19.18 read, at the time, as "no interface at all," not yet distinguished from "no write interface" — corrected by the milestone below). `engineering/testing/accounting/JournalEntries.postman_collection.json` exists (verified via Newman per that collection's own description) but Application Integration/API-Testing were not separately re-verified as their own recorded milestone step here.

**Completed (continued):** Ledger Read Model (General Ledger) — Business/Presentation/Integration/API-Testing layers. Preceded by a dedicated, code-free architecture review (Ch.19 Ledger, Ch.20 Journal, Ch.24 Trial Balance, Ch.33 Audit Trail, plus `03_ARCHITECTURE.md`'s CQRS-rejection Decision 5.9/§6.6.3/Ch.18 read-model chapters and Event-Sourcing-deferral Decision 3.7.3) that reached one frozen conclusion: **General Ledger is a read model over the existing `LedgerEntry` Aggregate — not a new Aggregate, not a new table, not a cross-module read-model projection** (Ch.6.6.3/Ch.18's async-Domain-Event-populated read model is scoped to cross-module reporting; this stays entirely within Accounting, reading Accounting's own data, so CQRS's rejection as a platform default, Decision 5.9, applies undiluted). Two frozen architectural clarifications made along the way: (1) Ch.19.1 defines a Ledger strictly as a **per-account** concept — there is no handbook notion of a cross-account, commingled raw entry feed, so `GET /ledger` (query-param `accountUuid`) and `GET /ledger/accounts/:accountUuid` (path-param) are the SAME operation, not two different features; (2) the prior milestone's own `index.ts` comment ("Ledger Entry has NO endpoints of its own") governed CREATION/EDITING only (Ch.19.18) — read-only `GET` endpoints do not conflict with it, corrected inline rather than treated as a handbook contradiction. Repository: `ILedgerRepository.listLedgerEntries` signature changed from `(tenantId, accountId?)` to a filter-object form (`accountId?`/`companyUuid?`/`dateFrom?`/`dateTo?`/`cursor?`/`limit`) returning `{entries, hasMore}` — safe because grep confirmed zero real callers of the old signature existed yet (only its own type declaration and a `jest.fn()` fixture stub), so this is additive, not a breaking redesign of a used contract; new `sumLedgerEntriesBefore` method (per-account debit/credit SUM aggregation, the same primitive Trial Balance/P&L/Balance Sheet will need, built once per this review's own "Shared Reporting Logic" recommendation). `IJournalEntryRepository` extended with one new `findJournalEntryByLineId` lookup — the smallest possible addition needed for Ch.19.11's mandatory drill-down, since `LedgerEntry` carries only the originating line's internal `id`, never the parent Journal Entry's `uuid`. Both are additive method changes to otherwise-frozen interfaces, not redesigns. `DecimalValue` (frozen Value Object) extended with `add`/`subtract` — its first real arithmetic callers, implemented via `BigInt` on a decimal-shifted integer representation (never floating point), needed because LDG-003's running balance must be computed at query time, never stored. Cursor pagination implemented exactly per `07_REST_API_STANDARDS.md` PAG-001..005: canonical ordering `entryDate` ascending then `uuid` ascending (a stable tie-breaker since every line of one multi-line Journal Entry shares an `entryDate`), an opaque base64url-encoded `{entryDate, uuid}` cursor (`business/ledger-cursor.ts`), default limit 25/max 100 (clamped, never rejected). **Two real bugs caught and fixed only by this milestone's own live end-to-end verification against real MySQL** (neither is visible to a mocked-repository unit test): (1) the opening-balance boundary must be skipped entirely — not called with `position: undefined` — on a truly unbounded first page, since "omit position" means "sum the entire history," which double-counted every entry against the page that was about to walk forward from zero; (2) when resuming from a pagination cursor, the opening-balance boundary must be **inclusive** of the cursor row (`uuid <= y`, not `uuid < y`) since that row was already consumed on the prior page and its own amount must be folded into the carried-forward balance — getting this backwards silently dropped one row's amount from every subsequent page's running balance. Business: `get-account-ledger.service.ts` (the core per-account Ledger view: resolves the Account, computes the opening balance, paginates entries, computes running balance per entry via a new shared `calculate-running-balance.service.ts` exposing `netBalance`/`isDebitNormal`/`calculateRunningBalance`), `get-ledger-entry.service.ts` (Ch.19.11 drill-down: a Ledger Entry plus its originating, resolved Journal Entry). Presentation: `GET /ledger` (query `accountUuid` required), `GET /ledger/accounts/:accountUuid`, `GET /ledger/entries/:ledgerEntryUuid` — all three read-only, no create/update/delete route exists for Ledger at all (structural, not a permission check). `response-envelope.ts`'s `sendData` extended with an optional `meta` param (additive, every existing 2/3-arg caller unaffected) to carry `meta.pagination` per `07_REST_API_STANDARDS.md` Ch.14.3's literal documented envelope shape. New error codes: `ACC_INVALID_LEDGER_CURSOR` (422), `ACC_INVALID_LEDGER_DATE_RANGE` (422); `ACC_LEDGER_ENTRY_NOT_FOUND` (404) reused as-is. Per-line/per-entry `accountId`/`journalEntryLineId`/internal `id`/`tenantId`/`createdBy` never serialized (PK-003) — a Ledger Entry row's own `uuid` is sufficient for a client to drill down, so a page of entries never pays a per-row Account-lookup join cost; the multi-account drill-down endpoint likewise omits `accountUuid` entirely (a genuine, flagged limitation — `IAccountingRepository`, frozen, has no by-internal-id Account lookup, only by-`uuid`/by-`code` — identical carried-forward reasoning to `journal-entry.response.dto.ts`'s own documented omission of each line's `accountId`). Verified via `npm run typecheck` (api + web), `npm run lint` (15 modules scanned), `npm test -w apps/api` (**800 tests, 128 suites** — new unit specs for `DecimalValue.add/subtract`, `ledger-cursor`, `calculate-running-balance`, `get-account-ledger`, `get-ledger-entry`, plus 22 new integration tests added to the existing `accounting.routes.spec.ts`, no regressions), `prisma validate`/`generate` (schema unchanged, no new migration — General Ledger reuses the existing `ledger_entries` table entirely), and live verification against the real running app (`tsx src/server.ts`) and local MySQL: full fixture flow (Account Group → Account → activate → Financial Year → open → Fiscal Period → 3 Journal Entries created and posted) then every endpoint exercised manually via `curl` (including the two bugs above, caught this way before the Postman collection was finalized) and again via `engineering/testing/accounting/Ledger.postman_collection.json` (new; 4 folders — Setup/Get Account Ledger/Get Ledger/Get Ledger Entry — 35 requests including a genuine 3-page cursor-chained pagination walk proving no skipped/duplicated rows and correct running-balance continuity across page boundaries). Two consecutive `npx newman run` invocations against the real running app and local MySQL: **35/35 requests, 140/140 assertions, 0 failures both times**, each run landing on a distinct generated `lgYear` fixture. Confirmed `/health`, unauthenticated `GET /api/v1/users`/`GET /api/v1/authorization/roles` (401), and `GET /api/v1/accounting/financial-years` (still 200) unaffected. Test data (2 Account Groups, 2 Accounts, 1 Financial Year, 1 Fiscal Period, 3 Journal Entries, 6 Ledger Entries per run, all under tenant 900001) cleaned up after each run via direct `DELETE`; confirmed zero rows remain. **Trial Balance, Profit & Loss, Balance Sheet, Cash Flow, and Financial Closing were explicitly not started**, per this milestone's own stop condition.

**Blocked:** _None_

**Next up:** Chart of Accounts — Business/Presentation/Integration/API-Testing layers remain, once requested (AGP-002/COA-002 type-consistency validation, COA-001 immutable-account-type-once-posted, COA-003 deactivation-blocked-while-non-zero-balance/open-transaction, and code-format/name-required validation all belong there). Financial Closing (Ch.32) is a separate, not-yet-started handbook chapter. Trial Balance (Ch.24), Profit & Loss (Ch.25), Balance Sheet (Ch.26), and Cash Flow (Ch.27) are the natural next milestones now that the Ledger Read Model exists — each is expected to reuse `calculate-running-balance.service.ts`'s `netBalance`/`isDebitNormal` primitives and `ILedgerRepository.sumLedgerEntriesBefore` rather than reimplementing per-account balance aggregation, per this milestone's own "Shared Reporting Logic" review. Products/Product Categories (which will consume Tax Group/Tax Rule and, eventually, Account via Posting Rules) and Sales/Purchases (which will consume tax calculation and post Journal Entries against Account) are separate, not-yet-started modules entirely. Open design questions carried forward: the platform-template-vs-Company-instance seeding workflow for standard Account Groups/Accounts (Ch.17.6/18.5), the Event Sourcing deferral (`03_ARCHITECTURE.md` Ch.3.7.3, explicitly revisited and reaffirmed-as-still-deferred by this milestone's own architecture review), Fixed Assets/Depreciation (no handbook chapter exists yet despite Ch.32.9 presupposing depreciation postings), the Company-Currency activation join table and Business Location (Ch.9), and the pre-existing platform-wide gap (not unique to Ledger, not fixed this milestone) that `listJournalEntries` and every other pre-Ledger `list*` endpoint in this module still returns an unbounded collection rather than the cursor pagination `07_REST_API_STANDARDS.md` PAG-001 requires everywhere. Frontend remains out of scope until requested.
