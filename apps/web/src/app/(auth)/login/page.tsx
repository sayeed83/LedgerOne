import { LoginScreen } from "@/modules/authentication/screens/LoginScreen";

// FLD-001: routing only — the real screen lives in modules/authentication/screens.
export default function LoginPage() {
  return <LoginScreen />;
}
